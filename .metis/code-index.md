# Code Index

> Generated: 2026-03-25T13:39:18Z | 81 files | Python, Rust

## Project Structure

```
├── crates/
│   ├── plissken-cli/
│   │   ├── src/
│   │   │   └── main.rs
│   │   └── tests/
│   │       └── integration_test.rs
│   └── plissken-core/
│       └── src/
│           ├── config.rs
│           ├── crossref.rs
│           ├── discover.rs
│           ├── docstring.rs
│           ├── error.rs
│           ├── lib.rs
│           ├── manifest.rs
│           ├── model.rs
│           ├── parser/
│           │   ├── mod.rs
│           │   ├── python.rs
│           │   ├── rust.rs
│           │   └── traits.rs
│           ├── render/
│           │   ├── crossref_renderer.rs
│           │   ├── docstring_renderer.rs
│           │   ├── mod.rs
│           │   ├── module/
│           │   │   ├── crossref.rs
│           │   │   ├── layout.rs
│           │   │   └── mod.rs
│           │   ├── module_renderer.rs
│           │   ├── renderer.rs
│           │   ├── ssg/
│           │   │   ├── mdbook.rs
│           │   │   ├── mkdocs.rs
│           │   │   ├── mod.rs
│           │   │   └── traits.rs
│           │   ├── templates.rs
│           │   └── theme.rs
│           └── test_fixtures.rs
└── tests/
    └── fixtures/
        ├── complex_generics/
        │   ├── python/
        │   │   └── types.py
        │   └── rust/
        │       ├── associated.rs
        │       ├── bounds.rs
        │       ├── const_generics.rs
        │       ├── lib.rs
        │       ├── lifetimes.rs
        │       ├── nested.rs
        │       └── pyo3_complex.rs
        ├── hybrid_binary/
        │   ├── python/
        │   │   ├── helpers.py
        │   │   └── hybrid_binary/
        │   │       ├── __init__.py
        │   │       ├── cli/
        │   │       │   └── __init__.py
        │   │       └── testing/
        │   │           └── __init__.py
        │   └── rust/
        │       ├── config.rs
        │       ├── decorators.rs
        │       ├── internal.rs
        │       ├── lib.rs
        │       └── utils.rs
        ├── minimal_hybrid/
        │   ├── python/
        │   │   └── pysnake/
        │   │       ├── __init__.py
        │   │       ├── handlers.py
        │   │       └── utils/
        │   │           ├── __init__.py
        │   │           └── validation.py
        │   └── rust/
        │       └── src/
        │           ├── handlers.rs
        │           ├── internal/
        │           │   ├── mod.rs
        │           │   └── parser.rs
        │           └── lib.rs
        ├── pure_python/
        │   ├── __init__.py
        │   ├── async_tasks.py
        │   ├── protocols.py
        │   ├── schedule.py
        │   ├── scheduler.py
        │   ├── task.py
        │   └── utils/
        │       ├── __init__.py
        │       ├── logging.py
        │       └── timing.py
        ├── pure_rust/
        │   └── src/
        │       ├── async_loader.rs
        │       ├── config.rs
        │       ├── error.rs
        │       ├── lib.rs
        │       ├── loader.rs
        │       ├── transform/
        │       │   ├── filters.rs
        │       │   ├── mod.rs
        │       │   └── pipeline.rs
        │       └── validate.rs
        └── separate_bindings/
            ├── bindings/
            │   └── src/
            │       └── lib.rs
            ├── core/
            │   └── src/
            │       ├── batch.rs
            │       ├── error.rs
            │       ├── lib.rs
            │       ├── pipeline.rs
            │       ├── stage.rs
            │       └── stream.rs
            └── python/
                ├── async_helpers.py
                └── helpers.py
```

## Modules

### crates/plissken-cli/src

> *Semantic summary to be generated by AI agent.*

#### crates/plissken-cli/src/main.rs

-  `Cli` struct L17-24 — `{ verbose: u8, command: Commands }` — plissken CLI - Documentation for the Rust-Python bridge
-  `verbose` macro L27-33 — `-` — Print a message if verbosity level is met
-  `warn_parse_error` function L39-43 — `(file_type: &str, file: &Path, error: &dyn std::fmt::Display)` — Emit a structured warning for parser errors.
-  `CliError` struct L69-73 — `{ message: String, hint: Option<String>, context: Option<String> }` — User-facing error with optional hint for recovery.
-  `CliError` type L75-93 — `= CliError` — plissken CLI - Documentation for the Rust-Python bridge
-  `new` function L76-82 — `(message: impl Into<String>) -> Self` — plissken CLI - Documentation for the Rust-Python bridge
-  `with_hint` function L84-87 — `(mut self, hint: impl Into<String>) -> Self` — plissken CLI - Documentation for the Rust-Python bridge
-  `with_context` function L89-92 — `(mut self, context: impl Into<String>) -> Self` — plissken CLI - Documentation for the Rust-Python bridge
-  `CliError` type L95-106 — `= CliError` — plissken CLI - Documentation for the Rust-Python bridge
-  `fmt` function L96-105 — `(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result` — plissken CLI - Documentation for the Rust-Python bridge
-  `CliError` type L108-112 — `= CliError` — plissken CLI - Documentation for the Rust-Python bridge
-  `fmt` function L109-111 — `(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result` — plissken CLI - Documentation for the Rust-Python bridge
-  `CliError` type L114 — `= CliError` — plissken CLI - Documentation for the Rust-Python bridge
-  `Commands` enum L117-162 — `Generate | Render | Init | Check` — plissken CLI - Documentation for the Rust-Python bridge
-  `main` function L164-182 — `() -> Result<()>` — plissken CLI - Documentation for the Rust-Python bridge
-  `generate` function L185-257 — `(path: &str, output: Option<PathBuf>, pretty: bool, verbosity: u8) -> Result<()>` — Generate documentation model from a project
-  `render` function L260-313 — `( path: &str, output_override: Option<PathBuf>, template_override: Option<String...` — Render documentation to Markdown files
-  `load_project_config` function L316-333 — `(path: &str, verbosity: u8) -> Result<(Config, PathBuf)>` — Load project configuration from path
-  `resolve_output_directory` function L336-350 — `( config: &Config, project_root: &Path, output_override: Option<PathBuf>, ) -> P...` — Resolve output directory from config and command-line override
-  `log_output_settings` function L353-358 — `(output_dir: &Path, template: Option<&str>, verbosity: u8)` — Log output settings at appropriate verbosity level
-  `parse_and_merge_modules` function L361-398 — `( config: &Config, project_root: &Path, verbosity: u8, ) -> Result<(Vec<PythonMo...` — Parse sources and merge synthesized modules
-  `create_renderer` function L401-410 — `(template: Option<&str>, project_root: &Path) -> Result<Renderer>` — Create renderer with theme
-  `create_output_directory` function L413-423 — `(output_dir: &Path) -> Result<()>` — Create output directory with helpful error messages
-  `resolve_content_directory` function L426-432 — `(output_dir: &Path, template: Option<&str>) -> PathBuf` — Determine content directory based on SSG type
-  `merge_synthesized_python_modules` function L435-517 — `( config: &Config, mut python_modules: Vec<PythonModule>, rust_modules: &[RustMo...` — Merge synthesized Python modules from Rust PyO3 bindings into existing modules
-  `synthesize_python_if_needed` function L524-542 — `( config: &Config, python_modules: Vec<PythonModule>, rust_modules: &[RustModule...` — Synthesize Python bindings from Rust if needed, or build cross-references
-  `build_cross_references` function L545-555 — `( config: &Config, python_modules: Vec<PythonModule>, rust_modules: &[RustModule...` — Build cross-references, synthesizing Python bindings if needed
-  `write_rendered_pages` function L558-618 — `( module_renderer: &ModuleRenderer, python_modules: &[PythonModule], rust_module...` — Write rendered module pages to disk
-  `generate_ssg_files` function L621-674 — `( module_renderer: &ModuleRenderer, python_modules: &[PythonModule], rust_module...` — Generate SSG-specific files (navigation, config, CSS)
-  `init` function L677-707 — `(force: bool, verbosity: u8) -> Result<()>` — Initialize a new plissken.toml configuration file
-  `ValidationIssue` struct L711-716 — `{ severity: String, message: String, hint: Option<String> }` — Validation issue found during check
-  `CliValidationResult` struct L720-724 — `{ valid: bool, config_path: String, issues: Vec<ValidationIssue> }` — Result of configuration validation (CLI output format)
-  `CliValidationResult` type L726-751 — `= CliValidationResult` — plissken CLI - Documentation for the Rust-Python bridge
-  `new` function L727-733 — `(config_path: &Path) -> Self` — plissken CLI - Documentation for the Rust-Python bridge
-  `add_error` function L735-742 — `(&mut self, message: impl Into<String>, hint: Option<String>)` — plissken CLI - Documentation for the Rust-Python bridge
-  `add_warning` function L744-750 — `(&mut self, message: impl Into<String>, hint: Option<String>)` — plissken CLI - Documentation for the Rust-Python bridge
-  `check` function L754-834 — `(path: &str, format: &str, verbosity: u8) -> Result<()>` — Validate configuration without generating documentation
-  `output_result` function L837-871 — `(result: &CliValidationResult, format: &str, verbosity: u8) -> Result<()>` — Output validation result in requested format
-  `DetectedProject` struct L874-883 — `{ name: String, has_rust: bool, has_python: bool, rust_crates: Vec<PathBuf>, rus...` — Detected project information
-  `DetectedProject` type L885-895 — `= DetectedProject` — plissken CLI - Documentation for the Rust-Python bridge
-  `description` function L886-894 — `(&self) -> String` — plissken CLI - Documentation for the Rust-Python bridge
-  `detect_project` function L898-983 — `() -> Result<DetectedProject>` — Detect project type from current directory
-  `extract_cargo_name` function L986-1008 — `(content: &str) -> Option<String>` — Extract package name from Cargo.toml content
-  `extract_pyproject_name` function L1011-1041 — `(content: &str) -> Option<String>` — Extract project name from pyproject.toml content
-  `extract_python_source` function L1044-1099 — `(content: &str) -> Option<PathBuf>` — Extract Python source directory from pyproject.toml content
-  `generate_config` function L1102-1154 — `(project: &DetectedProject) -> Result<String>` — Generate plissken.toml content
-  `find_config` function L1157-1192 — `(path: &Path) -> Result<PathBuf>` — Find the plissken.toml config file
-  `load_config` function L1195-1224 — `(path: &Path) -> Result<Config>` — Load and parse configuration file with helpful error messages
-  `extract_toml_location` function L1227-1237 — `(err: &str) -> Option<String>` — Extract line/column from toml error message
-  `summarize_toml_error` function L1240-1261 — `(err: &str) -> String` — Summarize a toml error message for users
-  `suggest_config_fix` function L1264-1281 — `(err: &str) -> String` — Suggest a fix based on the toml error
-  `parse_rust_sources` function L1284-1339 — `( config: &Config, project_root: &Path, verbosity: u8, ) -> Result<Vec<RustModul...` — Parse Rust source files based on config
-  `read_crate_name` function L1342-1379 — `(crate_dir: &Path) -> Result<String>` — Read the crate name from a Cargo.toml file
-  `file_to_module_path` function L1389-1432 — `(file_path: &Path, crate_name: &str, src_dir: &Path) -> String` — Convert a file path to a logical Rust module path
-  `find_rust_files` function L1435-1472 — `(dir: &Path) -> Result<Vec<PathBuf>>` — Find all Rust source files in a directory
-  `walk_dir` function L1452-1468 — `(dir: &Path, files: &mut Vec<PathBuf>) -> Result<()>` — plissken CLI - Documentation for the Rust-Python bridge
-  `parse_python_sources` function L1475-1531 — `( config: &Config, project_root: &Path, verbosity: u8, ) -> Result<Vec<PythonMod...` — Parse Python source files based on config
-  `find_python_files` function L1534-1563 — `(dir: &Path) -> Result<Vec<PathBuf>>` — Find all Python source files in a directory
-  `walk_dir` function L1537-1559 — `(dir: &Path, files: &mut Vec<PathBuf>) -> Result<()>` — plissken CLI - Documentation for the Rust-Python bridge
-  `get_project_version` function L1566-1621 — `(config: &Config, project_root: &Path) -> Option<String>` — Get project version from configured source
-  `get_git_ref` function L1624-1639 — `(project_root: &Path) -> Option<String>` — Get current git ref (branch or tag)
-  `get_git_commit` function L1642-1657 — `(project_root: &Path) -> Option<String>` — Get current git commit hash
-  `chrono_lite_now` function L1660-1711 — `() -> String` — Get current timestamp in ISO 8601 format (lightweight, no chrono dependency)
-  `is_leap_year` function L1713-1715 — `(year: i64) -> bool` — plissken CLI - Documentation for the Rust-Python bridge
-  `normalize_python_module_path` function L1720-1764 — `(file_path: &str, config: &Config, project_root: &Path) -> String` — Normalize a Python file path to a logical module path.

### crates/plissken-cli/tests

> *Semantic summary to be generated by AI agent.*

#### crates/plissken-cli/tests/integration_test.rs

-  `plissken_bin` function L8-17 — `() -> PathBuf` — Get the path to the plissken binary
-  `fixtures_path` function L20-27 — `() -> PathBuf` — Get path to test fixtures
-  `test_render_hybrid_binary_fixture` function L30-106 — `()` — Integration tests for the plissken CLI
-  `test_render_with_template_override` function L109-152 — `()` — Integration tests for the plissken CLI
-  `test_generate_json_output` function L155-194 — `()` — Integration tests for the plissken CLI
-  `test_render_creates_directory_structure` function L197-231 — `()` — Integration tests for the plissken CLI

### crates/plissken-core/src

> *Semantic summary to be generated by AI agent.*

#### crates/plissken-core/src/config.rs

- pub `DEFAULT_OUTPUT_FORMAT` variable L13 — `: &str` — Default output format for documentation
- pub `DEFAULT_OUTPUT_PATH` variable L16 — `: &str` — Default output path for generated documentation
- pub `DEFAULT_DOCS_RS_URL` variable L19 — `: &str` — Default docs.rs base URL for external links
- pub `DEFAULT_TEMPLATE` variable L22 — `: &str` — Default SSG template name
- pub `VERSION_SOURCE_CARGO` variable L25 — `: &str` — Version source identifier for Cargo.toml
- pub `VERSION_SOURCE_PYPROJECT` variable L28 — `: &str` — Version source identifier for pyproject.toml
- pub `CARGO_MANIFEST` variable L31 — `: &str` — Cargo manifest filename
- pub `PYPROJECT_MANIFEST` variable L34 — `: &str` — Python project manifest filename
- pub `PLISSKEN_CONFIG` variable L37 — `: &str` — Plissken configuration filename
- pub `TEMPLATE_MKDOCS_MATERIAL` variable L40 — `: &str` — MkDocs Material template name
- pub `TEMPLATE_MDBOOK` variable L43 — `: &str` — mdBook template name
- pub `DEFAULT_CRATES` variable L46 — `: &str` — Default crates configuration value
- pub `ConfigError` enum L50-65 — `NoLanguageConfigured | VersionSourceNotFound | RustCrateNotFound | PythonSourceN...` — Configuration validation error
- pub `ConfigWarning` struct L69-77 — `{ field: String, message: String, hint: Option<String> }` — Configuration warning (non-fatal issue)
- pub `new` function L81-87 — `(field: impl Into<String>, message: impl Into<String>) -> Self` — Create a new warning
- pub `with_hint` function L90-93 — `(mut self, hint: impl Into<String>) -> Self` — Add a hint to the warning
- pub `ValidationResult` struct L98-103 — `{ valid: bool, warnings: Vec<ConfigWarning> }` — Result of configuration validation
- pub `Config` struct L107-118 — `{ project: ProjectConfig, output: OutputConfig, rust: Option<RustConfig>, python...` — Root configuration from plissken.toml
- pub `ProjectConfig` struct L122-126 — `{ name: String, version_from: VersionSource }` — Project metadata
- pub `VersionSource` enum L135-140 — `Git | Cargo | Pyproject` — Where to get version information
- pub `OutputConfig` struct L144-151 — `{ format: String, path: PathBuf, template: Option<String> }` — Output configuration
- pub `RustConfig` struct L163-167 — `{ crates: Vec<PathBuf>, entry_point: Option<String> }` — Rust source configuration
- pub `PythonConfig` struct L171-183 — `{ package: String, source: Option<PathBuf>, auto_discover: bool, modules: HashMa...` — Python source configuration
- pub `ModuleSourceType` enum L188-191 — `Pyo3 | Python` — Source type for a Python module
- pub `LinksConfig` struct L195-200 — `{ dependencies: DependencySource, docs_rs_base: String }` — Linking configuration
- pub `DependencySource` enum L213-218 — `CargoLock | CargoToml | None` — Where to get dependency versions
- pub `QualityConfig` struct L222-229 — `{ require_docstrings: bool, min_coverage: Option<f64>, fail_on_broken_links: boo...` — Quality/linting configuration
- pub `from_file` function L238-260 — `(path: &std::path::Path) -> crate::error::Result<Self>` — Load configuration from a plissken.toml file.
- pub `with_inferred_defaults` function L277-318 — `(mut self, project_root: &Path) -> Self` — Apply inferred defaults from manifest files (Cargo.toml, pyproject.toml).
- pub `validate` function L332-357 — `(&self, project_root: &Path) -> Result<ValidationResult, ConfigError>` — Validate configuration semantically.
-  `ConfigWarning` type L79-94 — `= ConfigWarning` — Configuration for plissken projects
-  `default_version_from` function L128-130 — `() -> VersionSource` — Configuration for plissken projects
-  `default_format` function L153-155 — `() -> String` — Configuration for plissken projects
-  `default_output_path` function L157-159 — `() -> PathBuf` — Configuration for plissken projects
-  `default_dependencies` function L202-204 — `() -> DependencySource` — Configuration for plissken projects
-  `default_docs_rs` function L206-208 — `() -> String` — Configuration for plissken projects
-  `Config` type L231-489 — `= Config` — Configuration for plissken projects
-  `validate_version_source` function L359-393 — `(&self, project_root: &Path) -> Result<(), ConfigError>` — Configuration for plissken projects
-  `validate_rust_config` function L395-442 — `( &self, rust_config: &RustConfig, project_root: &Path, warnings: &mut Vec<Confi...` — Configuration for plissken projects
-  `validate_python_config` function L444-488 — `( &self, python_config: &PythonConfig, project_root: &Path, warnings: &mut Vec<C...` — Configuration for plissken projects
-  `tests` module L492-875 — `-` — Configuration for plissken projects
-  `minimal_config` function L497-513 — `() -> Config` — Configuration for plissken projects
-  `test_validate_no_language_configured` function L516-522 — `()` — Configuration for plissken projects
-  `test_validate_rust_crate_not_found` function L525-542 — `()` — Configuration for plissken projects
-  `test_validate_python_source_not_found` function L545-564 — `()` — Configuration for plissken projects
-  `test_validate_version_source_cargo_not_found` function L567-584 — `()` — Configuration for plissken projects
-  `test_validate_version_source_pyproject_not_found` function L587-606 — `()` — Configuration for plissken projects
-  `test_validate_valid_rust_config` function L609-630 — `()` — Configuration for plissken projects
-  `test_validate_valid_python_config` function L633-660 — `()` — Configuration for plissken projects
-  `test_validate_warnings_for_missing_init_py` function L663-692 — `()` — Configuration for plissken projects
-  `test_validate_warnings_for_missing_src_dir` function L695-721 — `()` — Configuration for plissken projects
-  `test_with_inferred_defaults_fills_project_name` function L724-737 — `()` — Configuration for plissken projects
-  `test_with_inferred_defaults_preserves_explicit_name` function L740-754 — `()` — Configuration for plissken projects
-  `test_with_inferred_defaults_fills_rust_config` function L757-787 — `()` — Configuration for plissken projects
-  `test_with_inferred_defaults_fills_python_config` function L790-819 — `()` — Configuration for plissken projects
-  `test_with_inferred_defaults_preserves_explicit_python_config` function L822-852 — `()` — Configuration for plissken projects
-  `test_with_inferred_defaults_pyproject_takes_precedence_for_name` function L855-874 — `()` — Configuration for plissken projects

#### crates/plissken-core/src/crossref.rs

- pub `CrossRefBuilder` struct L11-19 — `{ config: &'a Config, pyclass_map: HashMap<String, (String, String)>, pyfunction...` — Builds cross-references between Python and Rust items
- pub `new` function L23-30 — `(config: &'a Config) -> Self` — Create a new cross-reference builder
- pub `build` function L33-49 — `( mut self, rust_modules: &[RustModule], python_modules: Vec<PythonModule>, ) ->...` — Build cross-references and return updated modules and refs
- pub `build_cross_refs` function L215-222 — `( config: &Config, rust_modules: &[RustModule], python_modules: Vec<PythonModule...` — Build cross-references for a doc model
- pub `synthesize_python_from_rust` function L229-321 — `( rust_modules: &[RustModule], module_name: &str, ) -> (PythonModule, Vec<CrossR...` — Synthesize Python modules from Rust PyO3 bindings
- pub `synthesize_python_modules_from_rust` function L400-523 — `( rust_modules: &[RustModule], python_package: &str, rust_entry_point: &str, ) -...` — Synthesize Python modules from Rust modules with PyO3 bindings.
-  `index_rust_modules` function L52-97 — `(&mut self, modules: &[RustModule])` — Index all PyO3 items from Rust modules
-  `is_pyo3_module` function L100-107 — `(&self, module_path: &str) -> bool` — Check if a Python module is a PyO3 binding
-  `process_python_module` function L110-131 — `( &self, mut module: PythonModule, cross_refs: &mut Vec<CrossRef>, ) -> PythonMo...` — Process a Python module, updating items with Rust references
-  `process_python_item` function L134-187 — `( &self, item: PythonItem, module_path: &str, cross_refs: &mut Vec<CrossRef>, ) ...` — Process a single Python item
-  `process_python_method` function L190-211 — `( &self, mut method: PythonFunction, rust_struct_name: &str, rust_module: &str, ...` — Process a Python method, linking it to Rust
-  `synthesize_python_function` function L324-376 — `(rust_fn: &RustFunction, rust_path: &str) -> PythonFunction` — Synthesize a Python function from a Rust function
-  `synthesize_python_method` function L379-386 — `( rust_fn: &RustFunction, rust_struct: &str, rust_module: &str, ) -> PythonFunct...` — Synthesize a Python method from a Rust method in pymethods block
-  `rust_type_to_python` function L529-537 — `(rust_type: &str) -> String` — Convert a Rust type to Python type hint (best effort)
-  `rust_type_to_python_normalized` function L540-695 — `(s: &str) -> String` — Internal conversion after whitespace normalization
-  `split_generic_pair` function L699-712 — `(s: &str) -> Option<(&str, &str)>` — Split a generic pair like "String,PyObject" into ("String", "PyObject")
-  `split_tuple_elements` function L716-740 — `(s: &str) -> Vec<&str>` — Split tuple elements like "i32,String,String" into ["i32", "String", "String"]
-  `tests` module L743-1253 — `-` — implementations by matching PyO3 metadata.
-  `test_config` function L747-776 — `() -> Config` — implementations by matching PyO3 metadata.
-  `test_pyclass_matching` function L779-811 — `()` — implementations by matching PyO3 metadata.
-  `test_pyfunction_matching` function L814-837 — `()` — implementations by matching PyO3 metadata.
-  `test_pymethods_matching` function L840-881 — `()` — implementations by matching PyO3 metadata.
-  `test_pure_python_unchanged` function L884-901 — `()` — implementations by matching PyO3 metadata.
-  `test_name_fallback_to_rust_name` function L904-922 — `()` — implementations by matching PyO3 metadata.
-  `test_synthesize_python_from_rust` function L925-996 — `()` — implementations by matching PyO3 metadata.
-  `test_rust_type_to_python_primitives` function L999-1022 — `()` — implementations by matching PyO3 metadata.
-  `test_rust_type_to_python_generics` function L1025-1044 — `()` — implementations by matching PyO3 metadata.
-  `test_rust_type_to_python_pyo3_types` function L1047-1067 — `()` — implementations by matching PyO3 metadata.
-  `test_rust_type_to_python_pyo3_wrappers` function L1070-1085 — `()` — implementations by matching PyO3 metadata.
-  `test_rust_type_to_python_spaced_types` function L1088-1096 — `()` — implementations by matching PyO3 metadata.
-  `test_rust_type_to_python_references` function L1099-1105 — `()` — implementations by matching PyO3 metadata.
-  `test_rust_type_to_python_tuples` function L1108-1117 — `()` — implementations by matching PyO3 metadata.
-  `test_rust_type_to_python_nested_references` function L1120-1126 — `()` — implementations by matching PyO3 metadata.
-  `test_rust_type_to_python_slices` function L1129-1134 — `()` — implementations by matching PyO3 metadata.
-  `test_rust_type_to_python_complex_nested` function L1137-1144 — `()` — implementations by matching PyO3 metadata.
-  `test_synthesize_hybrid_binary_fixture` function L1147-1200 — `()` — implementations by matching PyO3 metadata.
-  `test_synthesize_separate_bindings_fixture` function L1203-1252 — `()` — implementations by matching PyO3 metadata.

#### crates/plissken-core/src/discover.rs

- pub `DiscoveredModule` struct L14-21 — `{ name: String, path: PathBuf, module_type: ModuleSourceType }` — A discovered Python module
- pub `discover_python_modules` function L50-80 — `( source_dir: &Path, package_name: &str, ) -> Result<Vec<DiscoveredModule>, std:...` — Discover Python modules by walking the filesystem.
- pub `merge_modules` function L219-234 — `( discovered: Vec<DiscoveredModule>, explicit: &HashMap<String, ModuleSourceType...` — Merge discovered modules with explicitly configured modules.
-  `SKIP_DIRS` variable L24-40 — `: &[&str]` — Directories to skip during discovery
-  `should_skip_entry` function L83-103 — `(entry: &walkdir::DirEntry) -> bool` — Check if an entry should be skipped during directory traversal.
-  `path_to_module` function L106-125 — `( file_path: &Path, source_dir: &Path, package_name: &str, ) -> Option<Discovere...` — Convert a file path to a Python module.
-  `path_to_module_name` function L133-176 — `(relative_path: &Path, package_name: &str) -> Option<String>` — Convert a relative file path to a dotted module name.
-  `detect_module_type` function L183-214 — `(file_path: &Path) -> ModuleSourceType` — Detect if a Python file is a PyO3 stub module.
-  `tests` module L237-382 — `-` — to dotted module names.
-  `test_path_to_module_name_simple` function L242-248 — `()` — to dotted module names.
-  `test_path_to_module_name_nested` function L251-257 — `()` — to dotted module names.
-  `test_path_to_module_name_init` function L260-266 — `()` — to dotted module names.
-  `test_path_to_module_name_subpackage_init` function L269-275 — `()` — to dotted module names.
-  `test_path_to_module_name_with_package_in_path` function L278-284 — `()` — to dotted module names.
-  `test_discover_python_modules` function L287-318 — `()` — to dotted module names.
-  `test_detect_module_type_python` function L321-330 — `()` — to dotted module names.
-  `test_detect_module_type_pyo3_marker` function L333-339 — `()` — to dotted module names.
-  `test_detect_module_type_native_import` function L342-348 — `()` — to dotted module names.
-  `test_merge_modules` function L351-381 — `()` — to dotted module names.

#### crates/plissken-core/src/docstring.rs

- pub `parse_docstring` function L14-28 — `(docstring: &str) -> ParsedDocstring` — Parse a docstring into structured form
- pub `empty` function L757-766 — `() -> Self` — Create an empty parsed docstring
- pub `is_empty` function L769-776 — `(&self) -> bool` — Check if the docstring has any content
- pub `parse_rust_doc` function L795-880 — `(doc: &str) -> ParsedDocstring` — Parse a Rust doc comment into structured form
-  `DocstringStyle` enum L31-35 — `Google | NumPy | Plain` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `detect_style` function L38-72 — `(docstring: &str) -> DocstringStyle` — Detect the docstring style based on section markers
-  `parse_google_style` function L75-132 — `(docstring: &str) -> ParsedDocstring` — Parse Google-style docstring
-  `parse_numpy_style` function L135-195 — `(docstring: &str) -> ParsedDocstring` — Parse NumPy-style docstring
-  `parse_plain` function L198-210 — `(docstring: &str) -> ParsedDocstring` — Parse plain docstring (no structured sections)
-  `extract_summary_and_description` function L213-276 — `(lines: &[&str]) -> (Option<String>, Option<String>, usize)` — Extract summary and description from the beginning of a docstring
-  `is_known_section` function L278-303 — `(name: &str) -> bool` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `parse_google_params` function L306-379 — `(lines: &[&str], start: usize) -> (Vec<ParamDoc>, usize)` — Parse Google-style Args/Parameters section
-  `parse_param_line` function L382-409 — `(line: &str) -> (String, Option<String>, String)` — Parse a Google-style parameter line: "name (type): description" or "name: description"
-  `parse_google_returns` function L412-461 — `(lines: &[&str], start: usize) -> (Option<ReturnDoc>, usize)` — Parse Google-style Returns section
-  `parse_google_raises` function L464-524 — `(lines: &[&str], start: usize) -> (Vec<RaisesDoc>, usize)` — Parse Google-style Raises section
-  `parse_google_examples` function L527-572 — `(lines: &[&str], start: usize) -> (Vec<String>, usize)` — Parse Google-style Examples section
-  `parse_numpy_params` function L575-638 — `(lines: &[&str], start: usize) -> (Vec<ParamDoc>, usize)` — Parse NumPy-style Parameters section
-  `parse_numpy_returns` function L641-694 — `(lines: &[&str], start: usize) -> (Option<ReturnDoc>, usize)` — Parse NumPy-style Returns section
-  `parse_numpy_raises` function L697-747 — `(lines: &[&str], start: usize) -> (Vec<RaisesDoc>, usize)` — Parse NumPy-style Raises section
-  `parse_numpy_examples` function L750-753 — `(lines: &[&str], start: usize) -> (Vec<String>, usize)` — Parse NumPy-style Examples section
-  `ParsedDocstring` type L755-777 — `= ParsedDocstring` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `extract_rust_summary` function L883-931 — `(lines: &[&str]) -> (Option<String>, Option<String>, usize)` — Extract summary and description from Rust doc before any # sections
-  `parse_markdown_header` function L934-947 — `(line: &str) -> Option<&str>` — Parse a markdown header line, returning the section name if found
-  `parse_rust_arguments` function L960-1022 — `(lines: &[&str], start: usize) -> (Vec<ParamDoc>, usize)` — Parse Rust-style Arguments section
-  `parse_rust_param_line` function L1026-1071 — `(line: &str) -> Option<(String, String)>` — Parse a single Rust parameter line
-  `parse_rust_returns` function L1074-1088 — `(lines: &[&str], start: usize) -> (Option<ReturnDoc>, usize)` — Parse Rust-style Returns section
-  `parse_rust_errors` function L1091-1184 — `(lines: &[&str], start: usize, error_kind: &str) -> (Vec<RaisesDoc>, usize)` — Parse Rust-style Errors/Panics section
-  `parse_rust_examples` function L1187-1229 — `(lines: &[&str], start: usize) -> (Vec<String>, usize)` — Parse Rust-style Examples section
-  `parse_rust_section_text` function L1232-1261 — `(lines: &[&str], start: usize) -> (String, usize)` — Parse a section as plain text until next section
-  `tests` module L1264-1854 — `-` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_empty` function L1268-1271 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_summary_only` function L1274-1281 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_summary_and_description` function L1284-1301 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_google_args` function L1304-1329 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_google_returns` function L1332-1344 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_google_returns_with_type` function L1347-1360 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_google_raises` function L1363-1379 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_google_examples` function L1382-1395 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_google_examples_with_code_fence` function L1398-1425 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_google_full` function L1428-1455 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_numpy_style` function L1458-1494 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_docstring` function L1497-1513 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_detect_google_style` function L1516-1525 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_detect_numpy_style` function L1528-1533 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_detect_plain_style` function L1536-1541 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_scheduler_docstring` function L1544-1568 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_fixture_method_docstring` function L1571-1599 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_doc_empty` function L1606-1609 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_doc_summary_only` function L1612-1621 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_doc_summary_and_description` function L1624-1644 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_doc_arguments` function L1647-1661 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_doc_arguments_backticks` function L1664-1677 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_doc_returns` function L1680-1692 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_doc_errors` function L1695-1708 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_doc_errors_with_types` function L1711-1725 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_doc_panics` function L1728-1740 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_doc_examples` function L1743-1758 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_doc_safety` function L1761-1780 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_doc_full` function L1783-1830 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_rust_doc_no_sections` function L1833-1844 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`
-  `test_parse_markdown_header` function L1847-1853 — `()` — - **Rust style**: `# Arguments`, `# Returns`, `# Errors`, `# Panics`, `# Examples`

#### crates/plissken-core/src/error.rs

- pub `PlisskenError` enum L15-142 — `ConfigNotFound | ConfigParse | ConfigValidation | Parse | FileRead | Template | ...` — The primary error type for plissken-core operations.
- pub `Result` type L145 — `= std::result::Result<T, PlisskenError>` — A specialized Result type for plissken operations.
- pub `rust_parse` function L203-210 — `(path: impl Into<PathBuf>, message: impl Into<String>) -> Self` — Create a parse error for a Rust file.
- pub `rust_parse_at` function L213-224 — `( path: impl Into<PathBuf>, line: usize, message: impl Into<String>, ) -> Self` — Create a parse error for a Rust file with line number.
- pub `python_parse` function L227-234 — `(path: impl Into<PathBuf>, message: impl Into<String>) -> Self` — Create a parse error for a Python file.
- pub `python_parse_at` function L237-248 — `( path: impl Into<PathBuf>, line: usize, message: impl Into<String>, ) -> Self` — Create a parse error for a Python file with line number.
- pub `file_read` function L251-258 — `(path: impl Into<PathBuf>, source: std::io::Error) -> Self` — Create a file read error.
- pub `io` function L261-266 — `(context: impl Into<String>, source: std::io::Error) -> Self` — Create an IO error with context.
- pub `config_not_found` function L269-271 — `(path: impl Into<PathBuf>) -> Self` — Create a config not found error.
- pub `discovery` function L274-279 — `(path: impl Into<PathBuf>, message: impl Into<String>) -> Self` — Create a discovery error.
-  `PlisskenError` type L151-158 — `= PlisskenError` — `tera::Error`, and other error types.
-  `from` function L152-157 — `(err: std::io::Error) -> Self` — `tera::Error`, and other error types.
-  `PlisskenError` type L160-167 — `= PlisskenError` — `tera::Error`, and other error types.
-  `from` function L161-166 — `(err: tera::Error) -> Self` — `tera::Error`, and other error types.
-  `PlisskenError` type L169-176 — `= PlisskenError` — `tera::Error`, and other error types.
-  `from` function L170-175 — `(err: toml::de::Error) -> Self` — `tera::Error`, and other error types.
-  `PlisskenError` type L179-185 — `= PlisskenError` — `tera::Error`, and other error types.
-  `from` function L180-184 — `(err: crate::config::ConfigError) -> Self` — `tera::Error`, and other error types.
-  `PlisskenError` type L188-195 — `= PlisskenError` — `tera::Error`, and other error types.
-  `from` function L189-194 — `(err: crate::manifest::ManifestError) -> Self` — `tera::Error`, and other error types.
-  `PlisskenError` type L201-280 — `= PlisskenError` — `tera::Error`, and other error types.
-  `tests` module L283-356 — `-` — `tera::Error`, and other error types.
-  `test_parse_error_display` function L287-292 — `()` — `tera::Error`, and other error types.
-  `test_parse_error_with_line` function L295-299 — `()` — `tera::Error`, and other error types.
-  `test_config_not_found_display` function L302-306 — `()` — `tera::Error`, and other error types.
-  `test_config_validation_error` function L309-315 — `()` — `tera::Error`, and other error types.
-  `test_io_error_conversion` function L318-322 — `()` — `tera::Error`, and other error types.
-  `test_template_error_conversion` function L325-332 — `()` — `tera::Error`, and other error types.
-  `test_discovery_error` function L335-339 — `()` — `tera::Error`, and other error types.
-  `test_file_read_error` function L342-347 — `()` — `tera::Error`, and other error types.
-  `test_crossref_error` function L350-355 — `()` — `tera::Error`, and other error types.

#### crates/plissken-core/src/lib.rs

- pub `config` module L35 — `-` — ```
- pub `crossref` module L38 — `-` — ```
- pub `discover` module L39 — `-` — ```
- pub `docstring` module L40 — `-` — ```
- pub `error` module L41 — `-` — ```
- pub `manifest` module L42 — `-` — ```
- pub `model` module L43 — `-` — ```
- pub `parser` module L44 — `-` — ```
- pub `render` module L45 — `-` — ```
- pub `test_fixtures` module L47 — `-` — ```
- pub `detail` module L102-151 — `-` — Advanced types for users who need fine-grained control over documentation.

#### crates/plissken-core/src/manifest.rs

- pub `ManifestError` enum L12-18 — `IoError | TomlError` — Errors that can occur when parsing manifest files
- pub `CargoManifest` struct L22-31 — `{ name: Option<String>, version: Option<String>, is_workspace: bool, workspace_m...` — Parsed Cargo.toml manifest
- pub `PyProjectManifest` struct L35-42 — `{ name: Option<String>, version: Option<String>, package_dir: Option<PathBuf> }` — Parsed pyproject.toml manifest
- pub `parse` function L98-101 — `(path: &Path) -> Result<Self, ManifestError>` — Parse a Cargo.toml file
- pub `parse_str` function L104-125 — `(content: &str) -> Result<Self, ManifestError>` — Parse Cargo.toml content from a string
- pub `parse` function L130-133 — `(path: &Path) -> Result<Self, ManifestError>` — Parse a pyproject.toml file
- pub `parse_str` function L136-170 — `(content: &str) -> Result<Self, ManifestError>` — Parse pyproject.toml content from a string
- pub `InferredConfig` struct L175-186 — `{ project_name: Option<String>, rust_crates: Option<Vec<PathBuf>>, rust_entry_po...` — Inferred configuration values from manifest files
- pub `from_directory` function L190-233 — `(project_root: &Path) -> Self` — Infer configuration from manifest files in the given directory
-  `CargoToml` struct L47-50 — `{ package: Option<CargoPackage>, workspace: Option<CargoWorkspace> }` — extract metadata that can be used to infer default configuration values.
-  `CargoPackage` struct L53-56 — `{ name: Option<String>, version: Option<String> }` — extract metadata that can be used to infer default configuration values.
-  `CargoWorkspace` struct L59-61 — `{ members: Option<Vec<String>> }` — extract metadata that can be used to infer default configuration values.
-  `PyProjectToml` struct L64-67 — `{ project: Option<PyProject>, tool: Option<PyProjectTool> }` — extract metadata that can be used to infer default configuration values.
-  `PyProject` struct L70-73 — `{ name: Option<String>, version: Option<String> }` — extract metadata that can be used to infer default configuration values.
-  `PyProjectTool` struct L76-79 — `{ setuptools: Option<SetuptoolsConfig>, maturin: Option<MaturinConfig> }` — extract metadata that can be used to infer default configuration values.
-  `SetuptoolsConfig` struct L82-85 — `{ package_dir: Option<std::collections::HashMap<String, String>> }` — extract metadata that can be used to infer default configuration values.
-  `MaturinConfig` struct L89-94 — `{ python_source: Option<String>, module_name: Option<String> }` — extract metadata that can be used to infer default configuration values.
-  `CargoManifest` type L96-126 — `= CargoManifest` — extract metadata that can be used to infer default configuration values.
-  `PyProjectManifest` type L128-171 — `= PyProjectManifest` — extract metadata that can be used to infer default configuration values.
-  `InferredConfig` type L188-234 — `= InferredConfig` — extract metadata that can be used to infer default configuration values.
-  `tests` module L237-428 — `-` — extract metadata that can be used to infer default configuration values.
-  `test_parse_cargo_toml_simple` function L241-252 — `()` — extract metadata that can be used to infer default configuration values.
-  `test_parse_cargo_toml_workspace` function L255-271 — `()` — extract metadata that can be used to infer default configuration values.
-  `test_parse_cargo_toml_workspace_only` function L274-286 — `()` — extract metadata that can be used to infer default configuration values.
-  `test_parse_pyproject_toml_simple` function L289-299 — `()` — extract metadata that can be used to infer default configuration values.
-  `test_parse_pyproject_toml_with_maturin` function L302-315 — `()` — extract metadata that can be used to infer default configuration values.
-  `test_parse_pyproject_toml_with_setuptools` function L318-330 — `()` — extract metadata that can be used to infer default configuration values.
-  `test_inferred_config_from_cargo_only` function L333-352 — `()` — extract metadata that can be used to infer default configuration values.
-  `test_inferred_config_from_pyproject_only` function L355-377 — `()` — extract metadata that can be used to infer default configuration values.
-  `test_inferred_config_hybrid_project` function L380-427 — `()` — extract metadata that can be used to infer default configuration values.

#### crates/plissken-core/src/model.rs

- pub `SourceType` enum L8-15 — `Python | PyO3Binding | Rust` — Source type indicator for Python modules
- pub `Visibility` enum L19-24 — `Public | PubCrate | PubSuper | Private` — Visibility level for Rust items
- pub `SourceLocation` struct L28-32 — `{ file: PathBuf, line_start: usize, line_end: usize }` — A reference to a location in source code
- pub `SourceSpan` struct L36-40 — `{ location: SourceLocation, source: String }` — Source code span with the actual text
- pub `RustModule` struct L44-50 — `{ path: String, doc_comment: Option<String>, parsed_doc: Option<ParsedDocstring>...` — A Rust module with its items
- pub `RustItem` enum L55-63 — `Struct | Enum | Function | Trait | Impl | Const | TypeAlias` — A Rust item (struct, enum, function, etc.)
- pub `RustStruct` struct L67-78 — `{ name: String, visibility: Visibility, doc_comment: Option<String>, parsed_doc:...` — A Rust struct definition
- pub `PyClassMeta` struct L82-85 — `{ name: Option<String>, module: Option<String> }` — PyO3 #[pyclass] metadata
- pub `RustField` struct L89-94 — `{ name: String, ty: String, visibility: Visibility, doc_comment: Option<String> ...` — A Rust field
- pub `RustEnum` struct L98-107 — `{ name: String, visibility: Visibility, doc_comment: Option<String>, parsed_doc:...` — A Rust enum definition
- pub `RustVariant` struct L111-115 — `{ name: String, doc_comment: Option<String>, fields: Vec<RustField> }` — A Rust enum variant
- pub `RustFunction` struct L119-135 — `{ name: String, visibility: Visibility, doc_comment: Option<String>, parsed_doc:...` — A Rust function definition
- pub `RustFunctionSig` struct L139-142 — `{ params: Vec<RustParam>, return_type: Option<String> }` — Rust function signature
- pub `RustParam` struct L146-150 — `{ name: String, ty: String, default: Option<String> }` — A Rust function parameter
- pub `PyFunctionMeta` struct L154-157 — `{ name: Option<String>, signature: Option<String> }` — PyO3 #[pyfunction] metadata
- pub `RustTrait` struct L161-173 — `{ name: String, visibility: Visibility, doc_comment: Option<String>, parsed_doc:...` — A Rust trait definition
- pub `RustAssociatedType` struct L177-184 — `{ name: String, doc_comment: Option<String>, generics: Option<String>, bounds: O...` — A Rust associated type in a trait
- pub `RustImpl` struct L188-201 — `{ generics: Option<String>, target: String, trait_: Option<String>, where_clause...` — A Rust impl block
- pub `RustConst` struct L205-212 — `{ name: String, visibility: Visibility, doc_comment: Option<String>, ty: String,...` — A Rust const item
- pub `RustTypeAlias` struct L216-224 — `{ name: String, visibility: Visibility, doc_comment: Option<String>, generics: O...` — A Rust type alias
- pub `PythonModule` struct L232-239 — `{ path: String, docstring: Option<String>, parsed_doc: Option<ParsedDocstring>, ...` — A Python module
- pub `PythonItem` enum L244-248 — `Class | Function | Variable` — A Python item
- pub `PythonClass` struct L252-262 — `{ name: String, docstring: Option<String>, parsed_doc: Option<ParsedDocstring>, ...` — A Python class
- pub `PythonFunction` struct L266-281 — `{ name: String, docstring: Option<String>, signature_str: String, signature: Pyt...` — A Python function
- pub `PythonFunctionSig` struct L285-288 — `{ params: Vec<PythonParam>, return_type: Option<String> }` — Python function signature
- pub `PythonParam` struct L292-296 — `{ name: String, ty: Option<String>, default: Option<String> }` — A Python function parameter
- pub `PythonVariable` struct L300-305 — `{ name: String, ty: Option<String>, value: Option<String>, docstring: Option<Str...` — A Python variable/attribute
- pub `ParsedDocstring` struct L309-316 — `{ summary: Option<String>, description: Option<String>, params: Vec<ParamDoc>, r...` — Parsed docstring (Google/NumPy style)
- pub `ParamDoc` struct L320-324 — `{ name: String, ty: Option<String>, description: String }` — Documented parameter
- pub `ReturnDoc` struct L328-331 — `{ ty: Option<String>, description: String }` — Documented return value
- pub `RaisesDoc` struct L335-338 — `{ ty: String, description: String }` — Documented exception
- pub `RustItemRef` struct L346-349 — `{ path: String, name: String }` — Reference to a Rust item
- pub `CrossRef` struct L353-357 — `{ python_path: String, rust_path: String, relationship: CrossRefKind }` — Cross-reference between Python and Rust
- pub `CrossRefKind` enum L361-368 — `Binding | Wraps | Delegates` — Kind of cross-reference
- pub `DocModel` struct L376-381 — `{ metadata: ProjectMetadata, rust_modules: Vec<RustModule>, python_modules: Vec<...` — The complete documentation model for a project
- pub `ProjectMetadata` struct L385-398 — `{ name: String, version: Option<String>, description: Option<String>, git_ref: O...` — Project metadata
- pub `test` function L406-412 — `(file: impl Into<PathBuf>, line_start: usize, line_end: usize) -> Self` — Create a test source location
- pub `test` function L417-422 — `(file: impl Into<PathBuf>, line_start: usize, line_end: usize) -> Self` — Create a test source span with empty source
- pub `new` function L425-435 — `( file: impl Into<PathBuf>, line_start: usize, line_end: usize, source: impl Int...` — Create a source span with actual source code
- pub `test` function L440-448 — `(path: impl Into<String>) -> Self` — Create a test Rust module
- pub `with_doc` function L450-453 — `(mut self, doc: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_item` function L455-458 — `(mut self, item: RustItem) -> Self` — Unified documentation model for Rust and Python items
- pub `with_source` function L460-463 — `(mut self, source: SourceSpan) -> Self` — Unified documentation model for Rust and Python items
- pub `test` function L468-480 — `(name: impl Into<String>) -> Self` — Create a test struct
- pub `with_visibility` function L482-485 — `(mut self, vis: Visibility) -> Self` — Unified documentation model for Rust and Python items
- pub `with_doc` function L487-490 — `(mut self, doc: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_generics` function L492-495 — `(mut self, generics: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_field` function L497-500 — `(mut self, field: RustField) -> Self` — Unified documentation model for Rust and Python items
- pub `with_derive` function L502-505 — `(mut self, derive: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_pyclass` function L507-510 — `(mut self, meta: PyClassMeta) -> Self` — Unified documentation model for Rust and Python items
- pub `with_source` function L512-515 — `(mut self, source: SourceSpan) -> Self` — Unified documentation model for Rust and Python items
- pub `test` function L520-527 — `(name: impl Into<String>, ty: impl Into<String>) -> Self` — Create a test field
- pub `private` function L529-532 — `(mut self) -> Self` — Unified documentation model for Rust and Python items
- pub `with_doc` function L534-537 — `(mut self, doc: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `test` function L542-561 — `(name: impl Into<String>) -> Self` — Create a test function
- pub `with_doc` function L563-566 — `(mut self, doc: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_generics` function L568-571 — `(mut self, generics: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_signature` function L573-576 — `(mut self, sig: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_param` function L578-581 — `(mut self, param: RustParam) -> Self` — Unified documentation model for Rust and Python items
- pub `with_return_type` function L583-586 — `(mut self, ty: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `async_` function L588-591 — `(mut self) -> Self` — Unified documentation model for Rust and Python items
- pub `unsafe_` function L593-596 — `(mut self) -> Self` — Unified documentation model for Rust and Python items
- pub `const_` function L598-601 — `(mut self) -> Self` — Unified documentation model for Rust and Python items
- pub `with_pyfunction` function L603-606 — `(mut self, meta: PyFunctionMeta) -> Self` — Unified documentation model for Rust and Python items
- pub `with_source` function L608-611 — `(mut self, source: SourceSpan) -> Self` — Unified documentation model for Rust and Python items
- pub `test` function L616-622 — `(name: impl Into<String>, ty: impl Into<String>) -> Self` — Create a test parameter
- pub `with_default` function L624-627 — `(mut self, default: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `new` function L631-636 — `() -> Self` — Unified documentation model for Rust and Python items
- pub `with_name` function L638-641 — `(mut self, name: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_module` function L643-646 — `(mut self, module: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `new` function L656-661 — `() -> Self` — Unified documentation model for Rust and Python items
- pub `with_name` function L663-666 — `(mut self, name: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_signature` function L668-671 — `(mut self, sig: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `test` function L682-691 — `(path: impl Into<String>) -> Self` — Create a test Python module
- pub `with_docstring` function L693-696 — `(mut self, doc: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_item` function L698-701 — `(mut self, item: PythonItem) -> Self` — Unified documentation model for Rust and Python items
- pub `pyo3_binding` function L703-706 — `(mut self) -> Self` — Unified documentation model for Rust and Python items
- pub `with_source` function L708-711 — `(mut self, source: SourceSpan) -> Self` — Unified documentation model for Rust and Python items
- pub `test` function L716-728 — `(name: impl Into<String>) -> Self` — Create a test Python class
- pub `with_docstring` function L730-733 — `(mut self, doc: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_base` function L735-738 — `(mut self, base: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_method` function L740-743 — `(mut self, method: PythonFunction) -> Self` — Unified documentation model for Rust and Python items
- pub `with_attribute` function L745-748 — `(mut self, attr: PythonVariable) -> Self` — Unified documentation model for Rust and Python items
- pub `with_decorator` function L750-753 — `(mut self, decorator: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_rust_impl` function L755-758 — `(mut self, rust_ref: RustItemRef) -> Self` — Unified documentation model for Rust and Python items
- pub `with_source` function L760-763 — `(mut self, source: SourceSpan) -> Self` — Unified documentation model for Rust and Python items
- pub `test` function L768-787 — `(name: impl Into<String>) -> Self` — Create a test Python function
- pub `with_docstring` function L789-792 — `(mut self, doc: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_signature` function L794-797 — `(mut self, sig: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_param` function L799-802 — `(mut self, param: PythonParam) -> Self` — Unified documentation model for Rust and Python items
- pub `with_return_type` function L804-807 — `(mut self, ty: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_decorator` function L809-812 — `(mut self, decorator: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `async_` function L814-817 — `(mut self) -> Self` — Unified documentation model for Rust and Python items
- pub `staticmethod` function L819-822 — `(mut self) -> Self` — Unified documentation model for Rust and Python items
- pub `classmethod` function L824-827 — `(mut self) -> Self` — Unified documentation model for Rust and Python items
- pub `property` function L829-832 — `(mut self) -> Self` — Unified documentation model for Rust and Python items
- pub `with_rust_impl` function L834-837 — `(mut self, rust_ref: RustItemRef) -> Self` — Unified documentation model for Rust and Python items
- pub `with_source` function L839-842 — `(mut self, source: SourceSpan) -> Self` — Unified documentation model for Rust and Python items
- pub `test` function L847-853 — `(name: impl Into<String>) -> Self` — Create a test parameter
- pub `with_type` function L855-858 — `(mut self, ty: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_default` function L860-863 — `(mut self, default: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `test` function L868-875 — `(name: impl Into<String>) -> Self` — Create a test variable
- pub `with_type` function L877-880 — `(mut self, ty: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_value` function L882-885 — `(mut self, value: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_docstring` function L887-890 — `(mut self, doc: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `new` function L894-899 — `(path: impl Into<String>, name: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `binding` function L903-909 — `(python_path: impl Into<String>, rust_path: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `wraps` function L911-917 — `(python_path: impl Into<String>, rust_path: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `delegates` function L919-925 — `(python_path: impl Into<String>, rust_path: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `test` function L930-937 — `(name: impl Into<String>) -> Self` — Create an empty doc model for testing
- pub `with_rust_module` function L939-942 — `(mut self, module: RustModule) -> Self` — Unified documentation model for Rust and Python items
- pub `with_python_module` function L944-947 — `(mut self, module: PythonModule) -> Self` — Unified documentation model for Rust and Python items
- pub `with_cross_ref` function L949-952 — `(mut self, cross_ref: CrossRef) -> Self` — Unified documentation model for Rust and Python items
- pub `test` function L957-966 — `(name: impl Into<String>) -> Self` — Create test metadata
- pub `with_version` function L968-971 — `(mut self, version: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_description` function L973-976 — `(mut self, desc: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_git_ref` function L978-981 — `(mut self, git_ref: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
- pub `with_git_commit` function L983-986 — `(mut self, commit: impl Into<String>) -> Self` — Unified documentation model for Rust and Python items
-  `SourceLocation` type L404-413 — `= SourceLocation` — Unified documentation model for Rust and Python items
-  `SourceSpan` type L415-436 — `= SourceSpan` — Unified documentation model for Rust and Python items
-  `RustModule` type L438-464 — `= RustModule` — Unified documentation model for Rust and Python items
-  `RustStruct` type L466-516 — `= RustStruct` — Unified documentation model for Rust and Python items
-  `RustField` type L518-538 — `= RustField` — Unified documentation model for Rust and Python items
-  `RustFunction` type L540-612 — `= RustFunction` — Unified documentation model for Rust and Python items
-  `RustParam` type L614-628 — `= RustParam` — Unified documentation model for Rust and Python items
-  `PyClassMeta` type L630-647 — `= PyClassMeta` — Unified documentation model for Rust and Python items
-  `PyClassMeta` type L649-653 — `impl Default for PyClassMeta` — Unified documentation model for Rust and Python items
-  `default` function L650-652 — `() -> Self` — Unified documentation model for Rust and Python items
-  `PyFunctionMeta` type L655-672 — `= PyFunctionMeta` — Unified documentation model for Rust and Python items
-  `PyFunctionMeta` type L674-678 — `impl Default for PyFunctionMeta` — Unified documentation model for Rust and Python items
-  `default` function L675-677 — `() -> Self` — Unified documentation model for Rust and Python items
-  `PythonModule` type L680-712 — `= PythonModule` — Unified documentation model for Rust and Python items
-  `PythonClass` type L714-764 — `= PythonClass` — Unified documentation model for Rust and Python items
-  `PythonFunction` type L766-843 — `= PythonFunction` — Unified documentation model for Rust and Python items
-  `PythonParam` type L845-864 — `= PythonParam` — Unified documentation model for Rust and Python items
-  `PythonVariable` type L866-891 — `= PythonVariable` — Unified documentation model for Rust and Python items
-  `RustItemRef` type L893-900 — `= RustItemRef` — Unified documentation model for Rust and Python items
-  `CrossRef` type L902-926 — `= CrossRef` — Unified documentation model for Rust and Python items
-  `DocModel` type L928-953 — `= DocModel` — Unified documentation model for Rust and Python items
-  `ProjectMetadata` type L955-987 — `= ProjectMetadata` — Unified documentation model for Rust and Python items
-  `tests` module L990-1102 — `-` — Unified documentation model for Rust and Python items
-  `test_model_serialization_roundtrip` function L994-1049 — `()` — Unified documentation model for Rust and Python items
-  `test_rust_struct_builder` function L1052-1069 — `()` — Unified documentation model for Rust and Python items
-  `test_python_function_builder` function L1072-1089 — `()` — Unified documentation model for Rust and Python items
-  `test_cross_ref_constructors` function L1092-1101 — `()` — Unified documentation model for Rust and Python items

#### crates/plissken-core/src/test_fixtures.rs

- pub `fixtures_root` function L22-29 — `() -> PathBuf` — Returns the path to the workspace's `tests/fixtures/` directory.
- pub `fixture_path` function L43-45 — `(relative_path: &str) -> PathBuf` — Returns the full path to a fixture file or directory.
- pub `hybrid_binary` module L51-73 — `-` — Fixture paths for the `hybrid_binary` test project.
- pub `rust_lib` function L55-57 — `() -> PathBuf` — Path to the Rust library source.
- pub `python_dir` function L60-62 — `() -> PathBuf` — Path to the Python package directory.
- pub `python_helpers` function L65-67 — `() -> PathBuf` — Path to the Python helpers module.
- pub `root` function L70-72 — `() -> PathBuf` — Path to the fixture root directory.
- pub `separate_bindings` module L79-96 — `-` — Fixture paths for the `separate_bindings` test project.
- pub `bindings_lib` function L83-85 — `() -> PathBuf` — Path to the bindings library source.
- pub `python_dir` function L88-90 — `() -> PathBuf` — Path to the Python package directory.
- pub `root` function L93-95 — `() -> PathBuf` — Path to the fixture root directory.
- pub `pure_python` module L101-123 — `-` — Fixture paths for the `pure_python` test project.
- pub `scheduler` function L105-107 — `() -> PathBuf` — Path to the scheduler module.
- pub `task` function L110-112 — `() -> PathBuf` — Path to the task module.
- pub `protocols` function L115-117 — `() -> PathBuf` — Path to the protocols module.
- pub `root` function L120-122 — `() -> PathBuf` — Path to the fixture root directory.
- pub `pure_rust` module L128-140 — `-` — Fixture paths for the `pure_rust` test project.
- pub `lib` function L132-134 — `() -> PathBuf` — Path to the library source.
- pub `root` function L137-139 — `() -> PathBuf` — Path to the fixture root directory.
- pub `complex_generics` module L146-158 — `-` — Fixture paths for the `complex_generics` test project.
- pub `root` function L150-152 — `() -> PathBuf` — Path to the fixture root directory.
- pub `python_types` function L155-157 — `() -> PathBuf` — Path to the Python types module.
- pub `minimal_hybrid` module L164-176 — `-` — Fixture paths for the `minimal_hybrid` test project.
- pub `root` function L168-170 — `() -> PathBuf` — Path to the fixture root directory.
- pub `python_dir` function L173-175 — `() -> PathBuf` — Path to the Python package directory.
-  `tests` module L179-206 — `-` — ```
-  `test_fixtures_root_exists` function L183-186 — `()` — ```
-  `test_hybrid_binary_paths` function L189-193 — `()` — ```
-  `test_pure_python_paths` function L196-199 — `()` — ```
-  `test_pure_rust_paths` function L202-205 — `()` — ```

### crates/plissken-core/src/parser

> *Semantic summary to be generated by AI agent.*

#### crates/plissken-core/src/parser/mod.rs

- pub `python` module L7 — `-` — This module provides parsers for extracting documentation from source code.
- pub `rust` module L8 — `-` — [`RustParser`] and [`PythonParser`] provide concrete implementations.
-  `traits` module L9 — `-` — [`RustParser`] and [`PythonParser`] provide concrete implementations.

#### crates/plissken-core/src/parser/python.rs

- pub `PythonParser` struct L8-10 — `{ parser: Parser }` — Python source code parser using tree-sitter
- pub `new` function L13-19 — `() -> Self` — Python source code parser using tree-sitter
- pub `parse_file` function L27-33 — `(&mut self, path: &Path) -> crate::error::Result<PythonModule>` — Parse a Python source file.
- pub `parse_str` function L40-71 — `(&mut self, content: &str, path: &Path) -> crate::error::Result<PythonModule>` — Parse Python source from a string.
-  `PythonParser` type L12-72 — `= PythonParser` — Python source code parser using tree-sitter
-  `PythonParser` type L74-78 — `impl Default for PythonParser` — Python source code parser using tree-sitter
-  `default` function L75-77 — `() -> Self` — Python source code parser using tree-sitter
-  `extract_module_docstring` function L84-101 — `(root: &Node, content: &str) -> Option<String>` — Python source code parser using tree-sitter
-  `extract_module_items` function L103-178 — `(root: &Node, content: &str, path: &Path) -> Vec<PythonItem>` — Python source code parser using tree-sitter
-  `extract_expression_string` function L181-189 — `(node: &Node, content: &str) -> Option<String>` — Extract a string from an expression_statement (used for variable docstrings)
-  `extract_class` function L191-232 — `(node: &Node, content: &str, path: &Path) -> PythonClass` — Python source code parser using tree-sitter
-  `extract_bases` function L234-259 — `(node: &Node, content: &str) -> Vec<String>` — Python source code parser using tree-sitter
-  `extract_class_body` function L261-331 — `( body: &Node, content: &str, path: &Path, ) -> (Option<String>, Vec<PythonFunct...` — Python source code parser using tree-sitter
-  `extract_function` function L333-392 — `(node: &Node, content: &str, path: &Path) -> PythonFunction` — Python source code parser using tree-sitter
-  `extract_parameters` function L394-505 — `(node: &Node, content: &str) -> (Vec<PythonParam>, String)` — Python source code parser using tree-sitter
-  `extract_function_docstring` function L507-524 — `(body: &Node, content: &str) -> Option<String>` — Python source code parser using tree-sitter
-  `extract_decorators` function L526-540 — `(node: &Node, content: &str) -> Vec<String>` — Python source code parser using tree-sitter
-  `extract_variable` function L542-564 — `(node: &Node, content: &str) -> Option<PythonVariable>` — Python source code parser using tree-sitter
-  `extract_string_content` function L566-596 — `(node: &Node, content: &str) -> Option<String>` — Python source code parser using tree-sitter
-  `dedent` function L600-631 — `(text: &str) -> String` — Remove common leading whitespace from all lines in a string.
-  `extract_source_text` function L633-637 — `(node: &Node, content: &str) -> String` — Python source code parser using tree-sitter
-  `node_text` function L639-643 — `(node: &Node, content: &str) -> String` — Python source code parser using tree-sitter
-  `PythonParser` type L649-673 — `= PythonParser` — Python source code parser using tree-sitter
-  `parse_file` function L650-652 — `(&mut self, path: &Path) -> crate::error::Result<super::traits::Module>` — Python source code parser using tree-sitter
-  `parse_str` function L654-660 — `( &mut self, content: &str, virtual_path: &Path, ) -> crate::error::Result<super...` — Python source code parser using tree-sitter
-  `language` function L662-664 — `(&self) -> super::traits::ParserLanguage` — Python source code parser using tree-sitter
-  `name` function L666-668 — `(&self) -> &'static str` — Python source code parser using tree-sitter
-  `extensions` function L670-672 — `(&self) -> &'static [&'static str]` — Python source code parser using tree-sitter
-  `tests` module L676-884 — `-` — Python source code parser using tree-sitter
-  `test_parse_empty` function L680-684 — `()` — Python source code parser using tree-sitter
-  `test_parse_module_docstring` function L687-700 — `()` — Python source code parser using tree-sitter
-  `test_parse_class` function L703-730 — `()` — Python source code parser using tree-sitter
-  `test_parse_function_with_types` function L733-754 — `()` — Python source code parser using tree-sitter
-  `test_parse_decorated_class` function L757-774 — `()` — Python source code parser using tree-sitter
-  `test_parse_property` function L777-798 — `()` — Python source code parser using tree-sitter
-  `test_parse_async_function` function L801-816 — `()` — Python source code parser using tree-sitter
-  `test_parse_pure_python_fixture` function L819-859 — `()` — Python source code parser using tree-sitter
-  `test_parse_enum_class` function L862-883 — `()` — Python source code parser using tree-sitter

#### crates/plissken-core/src/parser/rust.rs

- pub `RustParser` struct L13 — `-` — Rust source code parser using syn
- pub `new` function L16-18 — `() -> Self` — Rust source code parser using syn
- pub `parse_file` function L26-32 — `(&self, path: &Path) -> crate::error::Result<RustModule>` — Parse a Rust source file.
- pub `parse_str` function L39-73 — `(&self, content: &str, path: &Path) -> crate::error::Result<RustModule>` — Parse Rust source from a string.
-  `RustParser` type L15-328 — `= RustParser` — Rust source code parser using syn
-  `extract_item` function L75-88 — `(&self, item: &Item, content: &str, path: &Path) -> Option<RustItem>` — Rust source code parser using syn
-  `extract_struct` function L90-115 — `(&self, s: &ItemStruct, content: &str, path: &Path) -> RustStruct` — Rust source code parser using syn
-  `extract_enum` function L117-145 — `(&self, e: &ItemEnum, content: &str, path: &Path) -> RustEnum` — Rust source code parser using syn
-  `extract_function` function L147-159 — `(&self, f: &ItemFn, content: &str, path: &Path) -> RustFunction` — Rust source code parser using syn
-  `extract_trait` function L161-247 — `(&self, t: &ItemTrait, content: &str, path: &Path) -> RustTrait` — Rust source code parser using syn
-  `extract_impl` function L249-301 — `(&self, i: &ItemImpl, content: &str, path: &Path) -> RustImpl` — Rust source code parser using syn
-  `extract_const` function L303-314 — `(&self, c: &ItemConst, content: &str, path: &Path) -> RustConst` — Rust source code parser using syn
-  `extract_type_alias` function L316-327 — `(&self, t: &ItemType, content: &str, path: &Path) -> RustTypeAlias` — Rust source code parser using syn
-  `RustParser` type L330-334 — `impl Default for RustParser` — Rust source code parser using syn
-  `default` function L331-333 — `() -> Self` — Rust source code parser using syn
-  `convert_visibility` function L340-355 — `(vis: &SynVisibility) -> Visibility` — Rust source code parser using syn
-  `extract_doc_comments` function L357-383 — `(attrs: &[Attribute]) -> Option<String>` — Rust source code parser using syn
-  `extract_inner_doc_comments` function L385-411 — `(attrs: &[Attribute]) -> Option<String>` — Rust source code parser using syn
-  `extract_generics` function L413-444 — `(generics: &Generics) -> Option<String>` — Rust source code parser using syn
-  `extract_fields` function L446-471 — `(fields: &Fields) -> Vec<RustField>` — Rust source code parser using syn
-  `extract_derives` function L473-492 — `(attrs: &[Attribute]) -> Vec<String>` — Rust source code parser using syn
-  `extract_pyclass` function L494-519 — `(attrs: &[Attribute]) -> Option<PyClassMeta>` — Rust source code parser using syn
-  `extract_pyfunction` function L521-557 — `(attrs: &[Attribute]) -> Option<PyFunctionMeta>` — Rust source code parser using syn
-  `extract_function_common` function L559-629 — `( name: &str, vis: &SynVisibility, attrs: &[Attribute], sig: &syn::Signature, bl...` — Rust source code parser using syn
-  `get_source_span` function L631-656 — `( start: &proc_macro2::Span, end: &proc_macro2::Span, content: &str, path: &Path...` — Rust source code parser using syn
-  `RustParser` type L662-686 — `= RustParser` — Rust source code parser using syn
-  `parse_file` function L663-665 — `(&mut self, path: &Path) -> crate::error::Result<super::traits::Module>` — Rust source code parser using syn
-  `parse_str` function L667-673 — `( &mut self, content: &str, virtual_path: &Path, ) -> crate::error::Result<super...` — Rust source code parser using syn
-  `language` function L675-677 — `(&self) -> super::traits::ParserLanguage` — Rust source code parser using syn
-  `name` function L679-681 — `(&self) -> &'static str` — Rust source code parser using syn
-  `extensions` function L683-685 — `(&self) -> &'static [&'static str]` — Rust source code parser using syn
-  `tests` module L689-916 — `-` — Rust source code parser using syn
-  `test_parse_empty` function L693-697 — `()` — Rust source code parser using syn
-  `test_parse_struct` function L700-727 — `()` — Rust source code parser using syn
-  `test_parse_pyclass` function L730-748 — `()` — Rust source code parser using syn
-  `test_parse_function` function L751-771 — `()` — Rust source code parser using syn
-  `test_parse_impl_with_pymethods` function L774-801 — `()` — Rust source code parser using syn
-  `test_parse_module_doc` function L804-821 — `()` — Rust source code parser using syn
-  `test_parse_hybrid_binary_fixture` function L824-892 — `()` — Rust source code parser using syn
-  `test_parse_pure_rust_fixture` function L895-915 — `()` — Rust source code parser using syn

#### crates/plissken-core/src/parser/traits.rs

- pub `ParserLanguage` enum L12-17 — `Rust | Python` — Language identifier for parsers.
- pub `Module` enum L33-38 — `Rust | Python` — A parsed module, either Rust or Python.
- pub `path` function L42-47 — `(&self) -> &str` — Get the module path.
- pub `language` function L50-55 — `(&self) -> ParserLanguage` — Get the language of this module.
- pub `as_rust` function L58-63 — `(&self) -> Option<&RustModule>` — Try to get as a Rust module.
- pub `as_python` function L66-71 — `(&self) -> Option<&PythonModule>` — Try to get as a Python module.
- pub `into_rust` function L74-79 — `(self) -> Option<RustModule>` — Convert into a Rust module, if applicable.
- pub `into_python` function L82-87 — `(self) -> Option<PythonModule>` — Convert into a Python module, if applicable.
- pub `Parser` interface L121-162 — `{ fn parse_file(), fn parse_str(), fn language(), fn name(), fn extensions(), fn...` — A language-specific documentation parser.
- pub `create_parser` function L174-179 — `(language: ParserLanguage) -> Box<dyn Parser>` — Create a parser for the given language.
- pub `parser_for_extension` function L194-200 — `(ext: &str) -> Option<Box<dyn Parser>>` — Get a parser for the given file extension.
-  `ParserLanguage` type L19-26 — `= ParserLanguage` — for parsing source code from different languages.
-  `fmt` function L20-25 — `(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result` — for parsing source code from different languages.
-  `Module` type L40-88 — `= Module` — for parsing source code from different languages.
-  `Module` type L90-94 — `= Module` — for parsing source code from different languages.
-  `from` function L91-93 — `(m: RustModule) -> Self` — for parsing source code from different languages.
-  `Module` type L96-100 — `= Module` — for parsing source code from different languages.
-  `from` function L97-99 — `(m: PythonModule) -> Self` — for parsing source code from different languages.
-  `can_parse_extension` function L157-161 — `(&self, ext: &str) -> bool` — Check if this parser can handle the given file extension.
-  `tests` module L203-252 — `-` — for parsing source code from different languages.
-  `test_parser_language_display` function L207-210 — `()` — for parsing source code from different languages.
-  `test_module_conversions` function L213-220 — `()` — for parsing source code from different languages.
-  `test_create_parser` function L223-233 — `()` — for parsing source code from different languages.
-  `test_parser_for_extension` function L236-243 — `()` — for parsing source code from different languages.
-  `test_can_parse_extension` function L246-251 — `()` — for parsing source code from different languages.

### crates/plissken-core/src/render

> *Semantic summary to be generated by AI agent.*

#### crates/plissken-core/src/render/crossref_renderer.rs

- pub `link_to_rust` function L30-38 — `(rust_ref: &RustItemRef, from_python_path: &str) -> String` — Generate a relative Markdown link from a Python page to a Rust item.
- pub `link_to_python` function L60-70 — `(python_path: &str, from_rust_path: &str) -> String` — Generate a relative Markdown link from a Rust page to a Python item.
- pub `crossref_link` function L77-109 — `(xref: &CrossRef, from_path: &str, from_language: Language) -> CrossRefLink` — Generate a cross-reference link based on the CrossRef relationship.
- pub `CrossRefLink` struct L113-120 — `{ text: String, url: String, relationship: CrossRefKind }` — Represents a generated cross-reference link
- pub `to_markdown` function L124-126 — `(&self) -> String` — Render as a Markdown link
- pub `to_markdown_with_badge` function L129-136 — `(&self) -> String` — Render as a Markdown link with relationship indicator
- pub `Language` enum L141-144 — `Python | Rust` — Language enum for determining link direction
- pub `render_rust_impl_details` function L149-159 — `(rust_ref: &RustItemRef, from_python_path: &str) -> String` — Render a collapsible details block with a link to the Rust implementation.
- pub `render_python_exposure_details` function L164-174 — `(python_path: &str, from_rust_path: &str) -> String` — Render a collapsible details block with a link to the Python exposure.
-  `CrossRefLink` type L122-137 — `= CrossRefLink` — Python items and their Rust implementations, and vice versa.
-  `rust_path_to_file_path` function L183-185 — `(rust_path: &str) -> String` — Convert a Rust module path to a file path.
-  `python_path_to_file_path` function L190-192 — `(python_path: &str) -> String` — Convert a Python module path to a file path.
-  `split_python_path` function L197-203 — `(path: &str) -> (String, String)` — Split a Python path into module path and item name.
-  `split_rust_path` function L208-214 — `(path: &str) -> (String, String)` — Split a Rust path into module path and item name.
-  `item_to_anchor` function L219-221 — `(name: &str, item_type: &str) -> String` — Convert an item name to a Markdown anchor.
-  `compute_relative_path` function L226-238 — `(from_path: &str, to_path: &str) -> String` — Compute relative path from one documentation page to another.
-  `tests` module L241-437 — `-` — Python items and their Rust implementations, and vice versa.
-  `test_rust_path_to_file_path` function L246-252 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_python_path_to_file_path` function L255-264 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_split_python_path` function L267-275 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_split_rust_path` function L278-286 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_item_to_anchor` function L289-293 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_compute_relative_path_same_level` function L296-299 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_compute_relative_path_one_level_deep` function L302-305 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_compute_relative_path_two_levels_deep` function L308-311 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_link_to_rust` function L314-321 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_link_to_rust_nested_module` function L324-331 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_link_to_python` function L334-340 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_link_to_python_from_nested_rust` function L343-350 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_crossref_link_from_python` function L353-360 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_crossref_link_from_rust` function L363-370 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_crossref_link_markdown` function L373-383 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_crossref_link_wraps_relationship` function L386-393 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_crossref_link_delegates_relationship` function L396-403 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_render_rust_impl_details` function L406-415 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_render_python_exposure_details` function L418-426 — `()` — Python items and their Rust implementations, and vice versa.
-  `test_handles_missing_crossref_gracefully` function L429-436 — `()` — Python items and their Rust implementations, and vice versa.

#### crates/plissken-core/src/render/docstring_renderer.rs

- pub `render_docstring` function L42-76 — `(doc: &ParsedDocstring) -> String` — Render a parsed docstring to Markdown.
- pub `render_params_table` function L88-101 — `(params: &[ParamDoc]) -> String` — Render parameters as a Markdown table.
- pub `render_returns` function L111-122 — `(returns: &ReturnDoc) -> String` — Render returns section.
- pub `render_raises_table` function L134-145 — `(raises: &[RaisesDoc]) -> String` — Render raises/exceptions as a Markdown table.
- pub `render_examples` function L157-177 — `(examples: &[String]) -> String` — Render examples as fenced code blocks.
-  `dedent_code` function L183-209 — `(code: &str) -> String` — Remove common leading whitespace from all lines in a code block.
-  `detect_example_language` function L212-239 — `(example: &str) -> &'static str` — Detect the programming language of an example code block.
-  `escape_table_content` function L244-250 — `(content: &str) -> String` — Escape content for use in Markdown tables.
-  `tests` module L253-579 — `-` — raises tables, and code examples.
-  `test_parsed_docstring` function L257-291 — `() -> ParsedDocstring` — raises tables, and code examples.
-  `test_render_full_docstring` function L294-322 — `()` — raises tables, and code examples.
-  `test_render_params_only` function L325-346 — `()` — raises tables, and code examples.
-  `test_render_empty_docstring` function L349-361 — `()` — raises tables, and code examples.
-  `test_render_summary_only` function L364-376 — `()` — raises tables, and code examples.
-  `test_render_returns_without_type` function L379-389 — `()` — raises tables, and code examples.
-  `test_render_params_without_type` function L392-401 — `()` — raises tables, and code examples.
-  `test_escape_table_content` function L404-409 — `()` — raises tables, and code examples.
-  `test_detect_example_language_python` function L412-418 — `()` — raises tables, and code examples.
-  `test_detect_example_language_rust` function L421-427 — `()` — raises tables, and code examples.
-  `test_render_multiple_examples` function L430-438 — `()` — raises tables, and code examples.
-  `test_dedent_code` function L441-456 — `()` — raises tables, and code examples.
-  `test_render_examples_with_indented_code_fence` function L459-474 — `()` — raises tables, and code examples.
-  `test_render_raises_with_long_descriptions` function L477-489 — `()` — raises tables, and code examples.
-  `test_google_style_docstring_rendering` function L492-542 — `()` — raises tables, and code examples.
-  `test_numpy_style_docstring_rendering` function L545-578 — `()` — raises tables, and code examples.

#### crates/plissken-core/src/render/mod.rs

- pub `module` module L25 — `-` — ```
- pub `ssg` module L28 — `-` — ```
-  `crossref_renderer` module L23 — `-` — This module provides theme adapters and rendering utilities for generating
-  `docstring_renderer` module L24 — `-` — ```
-  `module_renderer` module L26 — `-` — ```
-  `renderer` module L27 — `-` — ```
-  `templates` module L29 — `-` — ```
-  `theme` module L30 — `-` — ```

#### crates/plissken-core/src/render/module_renderer.rs

- pub `RenderedPage` struct L22-27 — `{ path: PathBuf, content: String }` — Rendered output for a documentation file
- pub `ModuleRenderer` struct L95-98 — `{ renderer: &'a Renderer, linker: CrossRefLinker }` — Module page renderer that converts DocModel modules into Markdown files.
- pub `new` function L104-109 — `(renderer: &'a Renderer) -> Self` — Create a new module renderer
- pub `with_cross_refs` function L112-117 — `(renderer: &'a Renderer, cross_refs: Vec<CrossRef>) -> Self` — Create a module renderer with cross-references for bi-directional linking
- pub `render_python_module` function L127-148 — `( &self, module: &PythonModule, ) -> Result<Vec<RenderedPage>, tera::Error>` — Render a Python module to a single Markdown file with all content inline.
- pub `render_rust_module` function L811-837 — `( &self, module: &RustModule, ) -> Result<Vec<RenderedPage>, tera::Error>` — Render a Rust module to a single Markdown file with all content inline.
- pub `render_python_modules` function L1494-1504 — `( &self, modules: &[PythonModule], ) -> Result<Vec<RenderedPage>, tera::Error>` — Render all Python modules from a list
- pub `render_rust_modules` function L1507-1517 — `( &self, modules: &[RustModule], ) -> Result<Vec<RenderedPage>, tera::Error>` — Render all Rust modules from a list
- pub `generate_nav` function L1537-1544 — `( &self, adapter: &dyn super::ssg::SSGAdapter, python_modules: &[PythonModule], ...` — Generate navigation using an SSG adapter.
- pub `generate_config` function L1549-1556 — `( &self, adapter: &dyn super::ssg::SSGAdapter, title: &str, authors: &[String], ...` — Generate SSG config file using an adapter.
- pub `generate_custom_css` function L1561-1563 — `(&self, adapter: &dyn super::ssg::SSGAdapter) -> Option<String>` — Generate custom CSS using an adapter.
- pub `generate_nav_yaml` function L1580-1623 — `( &self, python_modules: &[PythonModule], rust_modules: &[RustModule], ) -> Stri...` — Generate navigation YAML for mkdocs.yml
- pub `generate_mdbook_summary` function L1645-1682 — `( &self, python_modules: &[PythonModule], rust_modules: &[RustModule], ) -> Stri...` — Generate SUMMARY.md for mdBook navigation
- pub `generate_mdbook_config` function L1687-1722 — `(&self, title: &str, authors: &[String]) -> String` — Generate book.toml configuration for mdBook
- pub `generate_mdbook_css` function L1728-1738 — `(&self) -> String` — Generate custom CSS for mdBook
-  `ModulePageBuilder` struct L34-36 — `{ content: String }` — Builder for constructing module documentation pages.
-  `ModulePageBuilder` type L38-92 — `= ModulePageBuilder` — and `RustModule` structures into Markdown documentation files.
-  `new` function L40-44 — `() -> Self` — Create a new page builder
-  `add_header` function L47-50 — `(&mut self, module_name: &str, badge: &str)` — Add the module header with a badge
-  `add_docstring` function L53-56 — `(&mut self, docstring: &crate::model::ParsedDocstring)` — Add a parsed docstring section
-  `add_section` function L59-61 — `(&mut self, title: &str)` — Add a section header (h2)
-  `add_item` function L64-67 — `(&mut self, item_content: &str)` — Add rendered item content with spacing
-  `add_variables_table` function L70-86 — `(&mut self, title: &str, items: &[T], row_renderer: F)` — Add a variables/constants table
-  `build` function L89-91 — `(self) -> String` — Build the final content
-  `render_python_module_inline` function L151-200 — `( &self, module: &PythonModule, classes: &[&PythonClass], functions: &[&PythonFu...` — Render a Python module with all content inline in a single page.
-  `render_python_class_inline` function L203-292 — `( &self, class: &PythonClass, module_path: &str, ) -> Result<String, tera::Error...` — Render a Python class inline (for single-page module format).
-  `render_python_function_inline` function L295-350 — `( &self, func: &PythonFunction, module_path: &str, ) -> Result<String, tera::Err...` — Render a Python function inline (for single-page module format).
-  `render_python_module_index` function L353-442 — `( &self, module: &PythonModule, classes: &[&PythonClass], functions: &[&PythonFu...` — Render a Python module index page with class cards
-  `render_python_class_page` function L445-540 — `( &self, class: &PythonClass, module_path: &str, module_dir: &str, ) -> Result<R...` — Render a single Python class as its own page
-  `render_python_function_page` function L544-610 — `( &self, func: &PythonFunction, module_path: &str, module_dir: &str, ) -> Result...` — Render Python module-level functions as their own page
-  `render_python_function_for_class_page` function L613-621 — `( &self, func: &PythonFunction, module_path: &str, _class_name: &str, parent_cla...` — Render a Python function/method for a class page (uses h4 for methods)
-  `render_python_function_with_context` function L627-707 — `( &self, func: &PythonFunction, heading_level: usize, module_path: &str, parent_...` — Render a Python function/method with class context
-  `format_python_params` function L710-727 — `(&self, params: &[PythonParam]) -> String` — Format Python parameters for display
-  `merge_params_with_docstring` function L733-757 — `( sig_params: &[PythonParam], docstring_params: &[ParamDoc], ) -> Vec<ParamDoc>` — Merge signature params with docstring params to get types + descriptions
-  `render_docstring_with_merged_params` function L763-780 — `( sig_params: &[PythonParam], docstring: &str, is_binding: bool, ) -> String` — Render docstring with merged parameters from signature
-  `is_rust_style_docstring` function L783-792 — `(docstring: &str) -> bool` — Detect if a docstring uses Rust-style markdown headers
-  `source_badge` function L795-801 — `(&self, source_type: &SourceType) -> Result<String, tera::Error>` — Get source type badge
-  `render_rust_module_inline` function L840-890 — `( &self, module: &RustModule, structs: &[&RustStruct], enums: &[&RustEnum], func...` — Render a Rust module with all content inline in a single page.
-  `render_rust_struct_inline` function L893-999 — `( &self, s: &RustStruct, impls: &[&RustImpl], module_path: &str, ) -> Result<Str...` — Render a Rust struct inline (for single-page module format).
-  `render_rust_enum_inline` function L1002-1039 — `( &self, e: &RustEnum, module_path: &str, ) -> Result<String, tera::Error>` — Render a Rust enum inline (for single-page module format).
-  `render_rust_function_inline` function L1042-1090 — `( &self, func: &RustFunction, module_path: &str, ) -> Result<String, tera::Error...` — Render a Rust function inline (for single-page module format).
-  `render_rust_module_index` function L1093-1194 — `( &self, module: &RustModule, structs: &[&RustStruct], enums: &[&RustEnum], func...` — Render a Rust module index page with struct/enum cards
-  `render_rust_struct_page` function L1197-1304 — `( &self, s: &RustStruct, impls: &[&RustImpl], module_path: &str, module_dir: &st...` — Render a single Rust struct as its own page
-  `render_rust_enum_page` function L1307-1349 — `( &self, e: &RustEnum, module_path: &str, module_dir: &str, ) -> Result<Rendered...` — Render a single Rust enum as its own page
-  `render_rust_function_page` function L1352-1404 — `( &self, func: &RustFunction, module_path: &str, module_dir: &str, ) -> Result<R...` — Render a single Rust function to its own page
-  `render_rust_function_with_context` function L1410-1477 — `( &self, f: &RustFunction, heading_level: usize, is_pymethod: bool, module_path:...` — Render a Rust function with pymethod context
-  `visibility_badge` function L1480-1487 — `(&self, vis: &Visibility) -> Result<String, tera::Error>` — Get visibility badge
-  `tests` module L1742-2109 — `-` — and `RustModule` structures into Markdown documentation files.
-  `test_renderer` function L1746-1748 — `() -> Renderer` — and `RustModule` structures into Markdown documentation files.
-  `find_page` function L1751-1755 — `(pages: &'a [RenderedPage], suffix: &str) -> Option<&'a RenderedPage>` — Helper to find a page by path suffix
-  `test_render_simple_python_module` function L1758-1782 — `()` — and `RustModule` structures into Markdown documentation files.
-  `test_render_python_module_with_class` function L1785-1812 — `()` — and `RustModule` structures into Markdown documentation files.
-  `test_render_python_module_pyo3_binding` function L1815-1834 — `()` — and `RustModule` structures into Markdown documentation files.
-  `test_render_python_module_with_cross_refs` function L1837-1865 — `()` — and `RustModule` structures into Markdown documentation files.
-  `test_render_simple_rust_module` function L1868-1891 — `()` — and `RustModule` structures into Markdown documentation files.
-  `test_render_rust_module_with_struct` function L1894-1936 — `()` — and `RustModule` structures into Markdown documentation files.
-  `test_render_rust_struct_with_pyclass` function L1939-1975 — `()` — and `RustModule` structures into Markdown documentation files.
-  `test_render_rust_function_badges` function L1978-1997 — `()` — and `RustModule` structures into Markdown documentation files.
-  `test_render_rust_enum` function L2000-2039 — `()` — and `RustModule` structures into Markdown documentation files.
-  `test_batch_render_modules` function L2042-2065 — `()` — and `RustModule` structures into Markdown documentation files.
-  `test_python_async_function` function L2068-2085 — `()` — and `RustModule` structures into Markdown documentation files.
-  `test_python_class_methods_types` function L2088-2108 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_tests` module L2122-2621 — `-` — Snapshot tests for rendered output using insta.
-  `test_renderer` function L2127-2129 — `() -> Renderer` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_python_module_simple` function L2136-2145 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_python_module_with_function` function L2148-2163 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_python_class_with_methods` function L2166-2208 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_python_async_function` function L2211-2227 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_python_pyo3_binding` function L2230-2265 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_python_enum_class` function L2268-2284 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_python_module_variables` function L2287-2308 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_rust_module_simple` function L2315-2324 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_rust_struct_with_fields` function L2327-2364 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_rust_enum_with_variants` function L2367-2397 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_rust_function_with_generics` function L2400-2414 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_rust_async_unsafe_function` function L2417-2432 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_rust_pyclass_struct` function L2435-2476 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_rust_pyfunction` function L2479-2499 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_complex_python_module` function L2506-2553 — `()` — and `RustModule` structures into Markdown documentation files.
-  `snapshot_complex_rust_module` function L2556-2620 — `()` — and `RustModule` structures into Markdown documentation files.

#### crates/plissken-core/src/render/renderer.rs

- pub `Renderer` struct L36-40 — `{ tera: Tera, theme: Box<dyn ThemeAdapter>, template_loader: TemplateLoader }` — Documentation renderer using Tera templates with SSG-native theming.
- pub `new` function L54-82 — `(template: Option<&str>, project_root: Option<&Path>) -> crate::error::Result<Se...` — Create a new renderer with the specified template theme.
- pub `has_user_override` function L85-87 — `(&self, template_name: &str) -> bool` — Check if a user override exists for the given template.
- pub `template_loader` function L90-92 — `(&self) -> &TemplateLoader` — Get the template loader for direct access to templates.
- pub `theme_name` function L95-97 — `(&self) -> &str` — Get the theme adapter name.
- pub `base_context` function L103-136 — `(&self) -> Context` — Create a base Tera context with theme values injected.
- pub `render_badge` function L145-156 — `( &self, text: &str, color_type: &str, badge_type: &str, ) -> Result<String, ter...` — Render a badge with the given text, color type, and semantic type.
- pub `badge_async` function L163-165 — `(&self) -> Result<String, tera::Error>` — Render an "async" badge (blue).
- pub `badge_unsafe` function L168-170 — `(&self) -> Result<String, tera::Error>` — Render an "unsafe" badge (red).
- pub `badge_deprecated` function L173-175 — `(&self) -> Result<String, tera::Error>` — Render a "deprecated" badge (yellow).
- pub `badge_visibility` function L182-189 — `(&self, visibility: &str) -> Result<String, tera::Error>` — Render a visibility badge.
- pub `badge_source` function L196-204 — `(&self, source_type: &str) -> Result<String, tera::Error>` — Render a source type badge with emoji.
- pub `render_code_block` function L213-224 — `( &self, code: &str, language: Option<&str>, caption: Option<&str>, ) -> Result<...` — Render a code block with optional language and caption.
- pub `render_signature` function L234-247 — `( &self, name: &str, params: &str, return_type: Option<&str>, is_async: bool, ) ...` — Render a function/method signature.
- pub `render_module` function L257-270 — `( &self, module_name: &str, description: &str, functions: &[String], classes: &[...` — Render a module documentation page.
- pub `render_template` function L275-290 — `( &self, template_name: &str, extra_context: &Context, ) -> Result<String, tera:...` — Render an arbitrary template with the given context.
-  `Renderer` type L42-291 — `= Renderer` — with theme adapters to generate styled documentation output.
-  `ThemeContext` struct L298-321 — `{ code_bg: String, code_fg: String, primary: String, accent: String, muted: Stri...` — Theme context for Tera templates.
-  `tests` module L324-596 — `-` — with theme adapters to generate styled documentation output.
-  `test_renderer_creation` function L328-337 — `()` — with theme adapters to generate styled documentation output.
-  `test_base_context_has_theme` function L340-350 — `()` — with theme adapters to generate styled documentation output.
-  `test_render_badge_mkdocs` function L353-362 — `()` — with theme adapters to generate styled documentation output.
-  `test_render_badge_colors` function L365-396 — `()` — with theme adapters to generate styled documentation output.
-  `test_badge_async` function L399-406 — `()` — with theme adapters to generate styled documentation output.
-  `test_badge_unsafe` function L409-416 — `()` — with theme adapters to generate styled documentation output.
-  `test_badge_deprecated` function L419-426 — `()` — with theme adapters to generate styled documentation output.
-  `test_badge_visibility` function L429-444 — `()` — with theme adapters to generate styled documentation output.
-  `test_badge_source` function L447-466 — `()` — with theme adapters to generate styled documentation output.
-  `test_badge_has_override_class` function L469-478 — `()` — with theme adapters to generate styled documentation output.
-  `test_render_code_block` function L481-491 — `()` — with theme adapters to generate styled documentation output.
-  `test_render_code_block_no_caption` function L494-504 — `()` — with theme adapters to generate styled documentation output.
-  `test_render_signature` function L507-523 — `()` — with theme adapters to generate styled documentation output.
-  `test_render_signature_async` function L526-534 — `()` — with theme adapters to generate styled documentation output.
-  `test_render_module` function L537-555 — `()` — with theme adapters to generate styled documentation output.
-  `test_render_module_no_functions` function L558-571 — `()` — with theme adapters to generate styled documentation output.
-  `test_theme_values_in_templates` function L574-595 — `()` — with theme adapters to generate styled documentation output.

#### crates/plissken-core/src/render/templates.rs

- pub `BADGE` variable L14 — `: &str` — User templates take precedence over bundled defaults on a per-file basis.
- pub `CODE_BLOCK` variable L15 — `: &str` — User templates take precedence over bundled defaults on a per-file basis.
- pub `SIGNATURE` variable L16 — `: &str` — User templates take precedence over bundled defaults on a per-file basis.
- pub `MODULE` variable L17 — `: &str` — User templates take precedence over bundled defaults on a per-file basis.
- pub `TemplateLoader` struct L40-45 — `{ bundled: HashMap<&'static str, &'static str>, user_dir: Option<PathBuf> }` — Template loader with user override support.
- pub `new` function L54-68 — `(project_root: Option<&Path>) -> Self` — Create a new template loader.
- pub `get` function L81-102 — `(&self, name: &str) -> crate::error::Result<String>` — Get a template by name.
- pub `has_user_override` function L105-111 — `(&self, name: &str) -> bool` — Check if a user override exists for the given template.
- pub `template_names` function L114-116 — `(&self) -> Vec<&'static str>` — List all available template names.
- pub `user_override_dir` function L119-121 — `(&self) -> Option<&Path>` — Get the user override directory, if configured and exists.
-  `bundled` module L13-18 — `-` — Bundled templates embedded at compile time.
-  `TemplateLoader` type L47-131 — `= TemplateLoader` — User templates take precedence over bundled defaults on a per-file basis.
-  `load_bundled` function L123-130 — `() -> HashMap<&'static str, &'static str>` — User templates take precedence over bundled defaults on a per-file basis.
-  `TemplateLoader` type L133-137 — `impl Default for TemplateLoader` — User templates take precedence over bundled defaults on a per-file basis.
-  `default` function L134-136 — `() -> Self` — User templates take precedence over bundled defaults on a per-file basis.
-  `tests` module L140-229 — `-` — User templates take precedence over bundled defaults on a per-file basis.
-  `test_load_bundled_templates` function L146-154 — `()` — User templates take precedence over bundled defaults on a per-file basis.
-  `test_bundled_template_content` function L157-167 — `()` — User templates take precedence over bundled defaults on a per-file basis.
-  `test_template_not_found` function L170-175 — `()` — User templates take precedence over bundled defaults on a per-file basis.
-  `test_user_override` function L178-204 — `()` — User templates take precedence over bundled defaults on a per-file basis.
-  `test_no_user_dir` function L207-217 — `()` — User templates take precedence over bundled defaults on a per-file basis.
-  `test_template_names` function L220-228 — `()` — User templates take precedence over bundled defaults on a per-file basis.

#### crates/plissken-core/src/render/theme.rs

- pub `ThemeAdapter` interface L31-124 — `{ fn code_bg(), fn code_fg(), fn primary(), fn accent(), fn muted(), fn border()...` — Trait for mapping semantic color names to SSG-specific CSS variables.
- pub `MkDocsMaterial` struct L133 — `-` — MkDocs Material theme adapter
- pub `MdBook` struct L181 — `-` — mdBook theme adapter
- pub `Minimal` struct L219 — `-` — Minimal theme adapter with hardcoded fallback colors
- pub `get_theme_adapter` function L280-289 — `(template: Option<&str>) -> Box<dyn ThemeAdapter>` — Get a theme adapter based on the template name from config.
-  `success` function L62-64 — `(&self) -> &str` — Success color (typically green) - for positive states
-  `warning` function L67-69 — `(&self) -> &str` — Warning color (typically yellow/orange) - for caution states
-  `error` function L72-74 — `(&self) -> &str` — Error color (typically red) - for error/danger states
-  `info` function L77-79 — `(&self) -> &str` — Info color (typically blue) - for informational states
-  `badge_async` function L86-88 — `(&self) -> &str` — Color for async badges
-  `badge_unsafe` function L91-93 — `(&self) -> &str` — Color for unsafe badges
-  `badge_deprecated` function L96-98 — `(&self) -> &str` — Color for deprecated badges
-  `badge_binding` function L101-103 — `(&self) -> &str` — Color for PyO3/binding badges
-  `badge_pub` function L106-108 — `(&self) -> &str` — Color for visibility pub badges
-  `badge_pub_crate` function L111-113 — `(&self) -> &str` — Color for visibility pub(crate) badges
-  `badge_rust` function L116-118 — `(&self) -> &str` — Color for Rust source badges
-  `badge_python` function L121-123 — `(&self) -> &str` — Color for Python source badges
-  `MkDocsMaterial` type L135-172 — `impl ThemeAdapter for MkDocsMaterial` — ```
-  `code_bg` function L136-138 — `(&self) -> &str` — ```
-  `code_fg` function L140-142 — `(&self) -> &str` — ```
-  `primary` function L144-146 — `(&self) -> &str` — ```
-  `accent` function L148-150 — `(&self) -> &str` — ```
-  `muted` function L152-154 — `(&self) -> &str` — ```
-  `border` function L156-158 — `(&self) -> &str` — ```
-  `name` function L160-162 — `(&self) -> &str` — ```
-  `badge_async` function L165-167 — `(&self) -> &str` — ```
-  `badge_binding` function L169-171 — `(&self) -> &str` — ```
-  `MdBook` type L183-211 — `impl ThemeAdapter for MdBook` — ```
-  `code_bg` function L184-186 — `(&self) -> &str` — ```
-  `code_fg` function L188-190 — `(&self) -> &str` — ```
-  `primary` function L192-194 — `(&self) -> &str` — ```
-  `accent` function L196-198 — `(&self) -> &str` — ```
-  `muted` function L200-202 — `(&self) -> &str` — ```
-  `border` function L204-206 — `(&self) -> &str` — ```
-  `name` function L208-210 — `(&self) -> &str` — ```
-  `Minimal` type L221-249 — `impl ThemeAdapter for Minimal` — ```
-  `code_bg` function L222-224 — `(&self) -> &str` — ```
-  `code_fg` function L226-228 — `(&self) -> &str` — ```
-  `primary` function L230-232 — `(&self) -> &str` — ```
-  `accent` function L234-236 — `(&self) -> &str` — ```
-  `muted` function L238-240 — `(&self) -> &str` — ```
-  `border` function L242-244 — `(&self) -> &str` — ```
-  `name` function L246-248 — `(&self) -> &str` — ```
-  `tests` module L292-462 — `-` — ```
-  `test_mkdocs_material_adapter` function L296-306 — `()` — ```
-  `test_mdbook_adapter` function L309-319 — `()` — ```
-  `test_minimal_adapter` function L322-333 — `()` — ```
-  `test_get_theme_adapter_mkdocs_material` function L336-349 — `()` — ```
-  `test_get_theme_adapter_mdbook` function L352-365 — `()` — ```
-  `test_get_theme_adapter_fallback` function L368-379 — `()` — ```
-  `test_adapters_return_valid_css` function L382-397 — `()` — ```
-  `test_semantic_colors` function L400-408 — `()` — ```
-  `test_badge_colors` function L411-430 — `()` — ```
-  `test_mkdocs_material_badge_colors` function L433-444 — `()` — ```
-  `test_badge_colors_default_to_semantic` function L447-461 — `()` — ```

### crates/plissken-core/src/render/module

> *Semantic summary to be generated by AI agent.*

#### crates/plissken-core/src/render/module/crossref.rs

- pub `CrossRefLinker` struct L81-83 — `{ cross_refs: Vec<CrossRef> }` — Generates cross-reference links between Python and Rust documentation.
- pub `new` function L87-89 — `(cross_refs: Vec<CrossRef>) -> Self` — Create a new CrossRefLinker with the given cross-references.
- pub `empty` function L92-94 — `() -> Self` — Create an empty CrossRefLinker (no cross-references).
- pub `has_refs` function L97-99 — `(&self) -> bool` — Check if any cross-references are available.
- pub `python_link_for_rust_method` function L109-149 — `( &self, rust_path: &str, method_name: &str, parent_struct: Option<&str>, ) -> O...` — Find cross-ref for a Rust method and generate link to Python counterpart (inline format).
- pub `python_link_for_rust_function` function L155-184 — `( &self, rust_path: &str, func_name: &str, ) -> Option<String>` — Find cross-ref for a Rust function and generate link to Python function (inline format).
- pub `python_link_for_rust_struct` function L194-223 — `( &self, rust_path: &str, struct_name: &str, ) -> Option<String>` — Find cross-ref for a Rust struct and generate link to Python class (inline format).
- pub `rust_link_for_python_function` function L235-265 — `( &self, python_path: &str, func_name: &str, ) -> Option<String>` — Find cross-ref for a Python function and generate link to Rust function (inline format).
- pub `rust_link_for_python_class` function L275-305 — `( &self, python_path: &str, class_name: &str, ) -> Option<String>` — Find cross-ref for a Python class and generate link to Rust struct (inline format).
- pub `rust_link_for_python_method` function L315-355 — `( &self, python_path: &str, method_name: &str, parent_class: Option<&str>, ) -> ...` — Find cross-ref for a Python method and generate link to Rust implementation (inline format).
-  `compute_python_page_path` function L17-26 — `(module_path: &str) -> String` — Compute the relative file path for a Python module page.
-  `compute_rust_page_path` function L32-34 — `(module_path: &str) -> String` — Compute the relative file path for a Rust module page.
-  `compute_rust_relative_prefix` function L39-42 — `(rust_path: &str) -> String` — Compute the `../` prefix to navigate from a Rust module to the root.
-  `compute_python_relative_prefix` function L48-52 — `(python_path: &str) -> String` — Compute the `../` prefix to navigate from a Python module to the root.
-  `CrossRefLinker` type L85-356 — `= CrossRefLinker` — between binding implementations and their Python APIs.
-  `tests` module L359-594 — `-` — between binding implementations and their Python APIs.
-  `test_cross_refs` function L363-381 — `() -> Vec<CrossRef>` — between binding implementations and their Python APIs.
-  `test_compute_python_page_path_single_segment` function L388-390 — `()` — between binding implementations and their Python APIs.
-  `test_compute_python_page_path_nested` function L393-402 — `()` — between binding implementations and their Python APIs.
-  `test_compute_rust_page_path_single_segment` function L405-407 — `()` — between binding implementations and their Python APIs.
-  `test_compute_rust_page_path_nested` function L410-419 — `()` — between binding implementations and their Python APIs.
-  `test_compute_rust_relative_prefix` function L422-432 — `()` — between binding implementations and their Python APIs.
-  `test_compute_python_relative_prefix` function L435-445 — `()` — between binding implementations and their Python APIs.
-  `test_empty_linker` function L452-460 — `()` — between binding implementations and their Python APIs.
-  `test_has_refs` function L463-466 — `()` — between binding implementations and their Python APIs.
-  `test_python_link_for_rust_struct` function L469-479 — `()` — between binding implementations and their Python APIs.
-  `test_python_link_for_rust_function` function L482-492 — `()` — between binding implementations and their Python APIs.
-  `test_python_link_for_rust_method` function L495-511 — `()` — between binding implementations and their Python APIs.
-  `test_rust_link_for_python_class` function L514-525 — `()` — between binding implementations and their Python APIs.
-  `test_rust_link_for_python_function` function L528-538 — `()` — between binding implementations and their Python APIs.
-  `test_rust_link_for_python_method` function L541-557 — `()` — between binding implementations and their Python APIs.
-  `test_nested_module_links` function L560-577 — `()` — between binding implementations and their Python APIs.
-  `test_no_match_returns_none` function L580-593 — `()` — between binding implementations and their Python APIs.

#### crates/plissken-core/src/render/module/layout.rs

- pub `PageLayout` struct L52 — `-` — Computes file paths and directory structure for documentation pages.
- pub `new` function L56-58 — `() -> Self` — Create a new PageLayout.
- pub `python_module_dir` function L76-79 — `(&self, module_path: &str) -> String` — Compute the directory path for a Python module.
- pub `python_index_path` function L82-84 — `(&self, module_path: &str) -> PathBuf` — Compute the path for a Python module's index page.
- pub `python_module_page` function L90-101 — `(&self, module_path: &str) -> PathBuf` — Compute the path for a Python module as a single page (inline format).
- pub `python_item_path` function L104-110 — `(&self, module_path: &str, item_name: &str) -> PathBuf` — Compute the path for a Python item (class, function) page.
- pub `python_module_depth` function L115-117 — `(&self, module_path: &str) -> usize` — Compute the depth of a Python module (number of dots).
- pub `rust_module_dir` function L135-138 — `(&self, module_path: &str) -> String` — Compute the directory path for a Rust module.
- pub `rust_index_path` function L141-143 — `(&self, module_path: &str) -> PathBuf` — Compute the path for a Rust module's index page.
- pub `rust_module_page` function L155-164 — `(&self, module_path: &str) -> PathBuf` — Compute the path for a Rust module as a single page (inline format).
- pub `rust_item_path` function L167-173 — `(&self, module_path: &str, item_name: &str) -> PathBuf` — Compute the path for a Rust item (struct, enum, function) page.
- pub `rust_module_depth` function L178-180 — `(&self, module_path: &str) -> usize` — Compute the depth of a Rust module (number of `::` separators).
- pub `python_link_from_rust` function L195-207 — `( &self, python_module: &str, item_name: &str, anchor: Option<&str>, ) -> String` — Compute a relative path from a Rust module to a Python item.
- pub `rust_link_from_python` function L218-230 — `( &self, rust_module: &str, item_name: &str, anchor: Option<&str>, ) -> String` — Compute a relative path from a Python module to a Rust item.
-  `PageLayout` type L54-231 — `= PageLayout` — filesystem organization.
-  `tests` module L234-362 — `-` — filesystem organization.
-  `test_python_module_dir` function L238-247 — `()` — filesystem organization.
-  `test_python_index_path` function L250-261 — `()` — filesystem organization.
-  `test_python_item_path` function L264-275 — `()` — filesystem organization.
-  `test_python_module_depth` function L278-284 — `()` — filesystem organization.
-  `test_rust_module_dir` function L287-296 — `()` — filesystem organization.
-  `test_rust_index_path` function L299-310 — `()` — filesystem organization.
-  `test_rust_item_path` function L313-324 — `()` — filesystem organization.
-  `test_rust_module_depth` function L327-333 — `()` — filesystem organization.
-  `test_python_link_from_rust` function L336-347 — `()` — filesystem organization.
-  `test_rust_link_from_python` function L350-361 — `()` — filesystem organization.

#### crates/plissken-core/src/render/module/mod.rs

-  `crossref` module L29 — `-` — This module contains extracted components from the monolithic ModuleRenderer,
-  `layout` module L30 — `-` — ```

### crates/plissken-core/src/render/ssg

> *Semantic summary to be generated by AI agent.*

#### crates/plissken-core/src/render/ssg/mdbook.rs

- pub `MdBookAdapter` struct L25 — `-` — mdBook adapter.
-  `MdBookAdapter` type L27-130 — `impl SSGAdapter for MdBookAdapter` — mdBook adapter implementation
-  `name` function L28-30 — `(&self) -> &'static str` — mdBook adapter implementation
-  `content_dir` function L32-34 — `(&self) -> &'static str` — mdBook adapter implementation
-  `nav_filename` function L36-38 — `(&self) -> &'static str` — mdBook adapter implementation
-  `generate_nav` function L40-74 — `(&self, python_modules: &[PythonModule], rust_modules: &[RustModule]) -> String` — mdBook adapter implementation
-  `generate_config` function L76-111 — `(&self, title: &str, authors: &[String]) -> Option<String>` — mdBook adapter implementation
-  `generate_custom_css` function L113-125 — `(&self) -> Option<String>` — mdBook adapter implementation
-  `custom_css_path` function L127-129 — `(&self) -> Option<&'static str>` — mdBook adapter implementation
-  `tests` module L133-212 — `-` — mdBook adapter implementation
-  `test_mdbook_adapter_properties` function L138-146 — `()` — mdBook adapter implementation
-  `test_generate_nav_empty` function L149-155 — `()` — mdBook adapter implementation
-  `test_generate_nav_python_only` function L158-167 — `()` — mdBook adapter implementation
-  `test_generate_nav_rust_only` function L170-179 — `()` — mdBook adapter implementation
-  `test_generate_config` function L182-192 — `()` — mdBook adapter implementation
-  `test_generate_config_no_authors` function L195-200 — `()` — mdBook adapter implementation
-  `test_generate_custom_css` function L203-211 — `()` — mdBook adapter implementation

#### crates/plissken-core/src/render/ssg/mkdocs.rs

- pub `MkDocsAdapter` struct L21 — `-` — MkDocs adapter for Material theme.
-  `MkDocsAdapter` type L23-82 — `impl SSGAdapter for MkDocsAdapter` — MkDocs adapter implementation
-  `name` function L24-26 — `(&self) -> &'static str` — MkDocs adapter implementation
-  `content_dir` function L28-30 — `(&self) -> &'static str` — MkDocs adapter implementation
-  `nav_filename` function L32-34 — `(&self) -> &'static str` — MkDocs adapter implementation
-  `generate_nav` function L36-75 — `(&self, python_modules: &[PythonModule], rust_modules: &[RustModule]) -> String` — MkDocs adapter implementation
-  `generate_config` function L77-81 — `(&self, _title: &str, _authors: &[String]) -> Option<String>` — MkDocs adapter implementation
-  `tests` module L85-125 — `-` — MkDocs adapter implementation
-  `test_mkdocs_adapter_properties` function L90-97 — `()` — MkDocs adapter implementation
-  `test_generate_nav_empty` function L100-106 — `()` — MkDocs adapter implementation
-  `test_generate_nav_python_only` function L109-118 — `()` — MkDocs adapter implementation
-  `test_generate_config_returns_none` function L121-124 — `()` — MkDocs adapter implementation

#### crates/plissken-core/src/render/ssg/mod.rs

- pub `get_ssg_adapter` function L51-56 — `(template: Option<&str>) -> Box<dyn SSGAdapter>` — Get an SSG adapter for the given template name.
-  `mdbook` module L23 — `-` — This module provides adapters for different static site generators,
-  `mkdocs` module L24 — `-` — ```
-  `traits` module L25 — `-` — ```
-  `tests` module L59-97 — `-` — ```
-  `test_get_ssg_adapter_mkdocs` function L63-68 — `()` — ```
-  `test_get_ssg_adapter_mdbook` function L71-76 — `()` — ```
-  `test_get_ssg_adapter_mdbook_variants` function L79-84 — `()` — ```
-  `test_get_ssg_adapter_default` function L87-90 — `()` — ```
-  `test_get_ssg_adapter_unknown` function L93-96 — `()` — ```

#### crates/plissken-core/src/render/ssg/traits.rs

- pub `NavEntry` struct L7-14 — `{ path: String, file_path: PathBuf, depth: usize }` — Entry for a module in the navigation
- pub `python_nav_entries` function L49-61 — `(modules: &[PythonModule]) -> Vec<NavEntry>` — Generate sorted navigation entries for Python modules
- pub `rust_nav_entries` function L64-76 — `(modules: &[RustModule]) -> Vec<NavEntry>` — Generate sorted navigation entries for Rust modules
- pub `SSGAdapter` interface L88-141 — `{ fn name(), fn content_dir(), fn nav_filename(), fn generate_nav(), fn generate...` — Adapter for static site generator output.
-  `python_module_page` function L20-31 — `(module_path: &str) -> PathBuf` — Compute the file path for a Python module page (inline format).
-  `rust_module_page` function L37-46 — `(module_path: &str) -> PathBuf` — Compute the file path for a Rust module page (inline format).
-  `generate_custom_css` function L121-123 — `(&self) -> Option<String>` — Generate custom CSS for the SSG, if any.
-  `custom_css_path` function L128-130 — `(&self) -> Option<&'static str>` — Path for custom CSS file, if generated.
-  `content_extension` function L133-135 — `(&self) -> &'static str` — File extension for content files.
-  `supports_nested_nav` function L138-140 — `(&self) -> bool` — Whether this SSG supports nested/hierarchical navigation.

### tests/fixtures/complex_generics/python

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/complex_generics/python/types.py

- pub `process_nested` function L74-85 — `def process_nested( data: list[dict[str, T | None]], ) -> list[T]` — Process deeply nested generic structures.
- pub `deep_nesting` function L88-103 — `def deep_nesting( data: dict[K, list[tuple[str, dict[str, list[V]]]]], ) -> Iter...` — Handle very deeply nested types.
- pub `parse_json` function L110-120 — `def parse_json(text: str) -> JSON` — Parse JSON with recursive type.
- pub `apply_callback` function L127-140 — `def apply_callback( func: Callable[[Mapping[str, Any]], Sequence[tuple[int, str]...` — Apply a callback with complex signature.
- pub `higher_order` function L143-156 — `def higher_order( f: Callable[[Callable[[T], U]], Callable[[U], V]], g: Callable...` — Higher-order function with nested callables.
- pub `decorator_with_extra_arg` function L163-179 — `def decorator_with_extra_arg( extra: str, ) -> Callable[[Callable[P, R]], Callab...` — Decorator that adds an extra first argument.
- pub `log_calls` function L182-194 — `def log_calls(func: Callable[P, R]) -> Callable[P, R]` — Decorator preserving full signature.
- pub `SupportsLessThan` class L202-207 — `(Protocol)` — Protocol for types supporting < comparison.
- pub `__lt__` method L205-207 — `def __lt__(self, other: Self) -> bool` — Compare with another instance.
- pub `SupportsAdd` class L210-215 — `(Protocol[T_co])` — Protocol for types supporting addition.
- pub `__add__` method L213-215 — `def __add__(self, other: Self) -> T_co` — Add two instances.
- pub `SupportsGetItem` class L218-223 — `(Protocol[K, V_co])` — Protocol for subscriptable types.
- pub `__getitem__` method L221-223 — `def __getitem__(self, key: K) -> V_co` — Get item by key.
- pub `AsyncIteratorProtocol` class L229-238 — `(Protocol[T_co])` — Protocol for async iterators.
- pub `__aiter__` method L232-234 — `def __aiter__(self) -> Self` — Return self.
- pub `__anext__` method L236-238 — `def __anext__(self) -> T_co` — Get next item.
- pub `UserDict` class L245-251 — `(TypedDict)` — User data structure.
- pub `ConfigDict` class L254-261 — `(TypedDict, total=False)` — Configuration with all optional fields.
- pub `NestedConfig` class L264-269 — `(TypedDict)` — Config with nested TypedDicts.
- pub `DatabaseConfig` class L272-279 — `(TypedDict)` — Database configuration.
- pub `CacheConfig` class L282-287 — `(TypedDict, total=False)` — Cache configuration.
- pub `Container` class L294-344 — `(Generic[T]) { __init__, add, get, map }` — Generic container class.
- pub `__init__` method L301-307 — `def __init__(self, items: Iterable[T] | None = None) -> None` — Initialize container.
- pub `add` method L309-319 — `def add(self, item: T) -> Self` — Add an item.
- pub `get` method L321-333 — `def get(self, index: int) -> T` — Get item by index.
- pub `map` method L335-344 — `def map(self, func: Callable[[T], U]) -> "Container[U]"` — Map a function over items.
- pub `BiMap` class L350-379 — `(Generic[K, V]) { __init__, put, get_by_key, get_by_value }` — Bidirectional map.
- pub `__init__` method L358-361 — `def __init__(self) -> None` — Initialize empty bimap.
- pub `put` method L363-371 — `def put(self, key: K, value: V) -> None` — Add a mapping.
- pub `get_by_key` method L373-375 — `def get_by_key(self, key: K) -> V | None` — Get value by key.
- pub `get_by_value` method L377-379 — `def get_by_value(self, value: V) -> K | None` — Get key by value.
- pub `fetch` function L395-408 — `def fetch(url: str, *, as_json: bool = False) -> dict[str, Any] | str` — Fetch URL content.
- pub `is_string_list` function L415-424 — `def is_string_list(val: list[Any]) -> TypeGuard[list[str]]` — Check if all items are strings.
- pub `is_not_none` function L427-436 — `def is_not_none(val: T | None) -> TypeGuard[T]` — Check if value is not None.
- pub `async_process` function L443-456 — `def async_process( items: Iterable[T], handler: Callable[[T], Awaitable[U]], ) -...` — Process items asynchronously.
- pub `create_async_generator` function L459-474 — `def create_async_generator( start: int, end: int, ) -> Coroutine[Any, Any, Gener...` — Create an async generator.

### tests/fixtures/complex_generics/rust

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/complex_generics/rust/associated.rs

- pub `Container` interface L6-20 — `{ fn get(), fn len(), fn is_empty() }` — Trait with associated type.
- pub `first_item` function L23-28 — `(container: &C) -> Option<&C::Item>` — Using associated types in function bounds.
- pub `collect_items` function L31-43 — `(container: &C) -> Vec<I>` — Complex associated type constraints.
- pub `Graph` interface L46-55 — `{ fn nodes(), fn edges(), fn neighbors() }` — Trait with multiple associated types.
- pub `LendingIterator` interface L60-68 — `{ fn next() }` — Generic Associated Types (GATs).
- pub `BorrowedContainer` interface L71-77 — `{ fn borrow() }` — Another GAT example - container with borrowed views.
- pub `WindowedSlice` struct L80-84 — `{ data: Vec<T>, window_size: usize, position: usize }` — Implementation of GAT trait.
- pub `consume_lending` function L101-111 — `(mut iter: I) -> usize` — Function using GAT-bounded parameter.
- pub `Mappable` interface L114-124 — `{ fn map() }` — Associated type with where clause.
-  `is_empty` function L17-19 — `(&self) -> bool` — Check if empty.
-  `Item` type L87 — `= &'a [T]` — Associated types and GATs.
-  `next` function L89-97 — `(&'a mut self) -> Option<Self::Item<'a>>` — Associated types and GATs.

#### tests/fixtures/complex_generics/rust/bounds.rs

- pub `multi_bound` function L8-13 — `(value: T) -> String` — Multiple trait bounds on single parameter.
- pub `interdependent` function L16-24 — `(a: T, b: U) -> V` — Multiple generic parameters with interdependent bounds.
- pub `numeric_ops` function L27-32 — `(a: T, b: T) -> T` — Arithmetic trait bounds.
- pub `with_associated` function L35-43 — `(iter: I) -> Vec<T>` — Bounds with associated type constraints.
- pub `NotCopy` interface L46 — `-` — Negative trait bounds simulation (using marker traits).
- pub `Serializable` interface L50-55 — `{ fn serialize(), fn deserialize() }` — Supertraits - trait that requires other traits.
- pub `roundtrip` function L58-61 — `(value: T) -> Result<T, DeserializeError>` — Function using supertrait.
- pub `complex_where` function L64-79 — `( a: A, b: B, c: C, d: D, e: E, ) -> (A, B, C, D, E)` — Complex where clause spanning multiple lines.
- pub `DeserializeError` struct L83 — `-` — Complex trait bounds.

#### tests/fixtures/complex_generics/rust/const_generics.rs

- pub `Buffer` struct L7-10 — `{ data: [u8; N], len: usize }` — Fixed-size buffer using const generics.
- pub `new` function L14-19 — `() -> Self` — Create a new empty buffer.
- pub `capacity` function L22-24 — `(&self) -> usize` — Get the capacity.
- pub `len` function L27-29 — `(&self) -> usize` — Get the current length.
- pub `is_empty` function L32-34 — `(&self) -> bool` — Check if empty.
- pub `push` function L37-45 — `(&mut self, byte: u8) -> bool` — Push a byte, returns false if full.
- pub `as_slice` function L48-50 — `(&self) -> &[u8]` — Get as slice.
- pub `Matrix` struct L61-63 — `{ data: [[T; COLS]; ROWS] }` — Matrix with const dimensions.
- pub `new` function L67-71 — `() -> Self` — Create a matrix filled with default values.
- pub `dimensions` function L74-76 — `(&self) -> (usize, usize)` — Get the dimensions.
- pub `get` function L79-81 — `(&self, row: usize, col: usize) -> Option<&T>` — Get a reference to an element.
- pub `get_mut` function L84-86 — `(&mut self, row: usize, col: usize) -> Option<&mut T>` — Get a mutable reference to an element.
- pub `matmul` function L98-116 — `( a: &Matrix<T, M, N>, b: &Matrix<T, N, P>, ) -> Matrix<T, M, P>` — Matrix multiplication with const generics.
- pub `Padded` struct L119-125 — `{ data: [T; N], _padding: [u8; ALIGN - (N % ALIGN)] }` — Const generic with expression.
- pub `transfer` function L128-142 — `( src: &Buffer<SRC>, ) -> Option<Buffer<DST>>` — Multiple const parameters with constraints.
- pub `RingBuffer` struct L145-149 — `{ data: [Option<T>; N], head: usize, tail: usize }` — Const generics with default values.
- pub `new` function L153-162 — `() -> Self` — Create a new ring buffer.
-  `default` function L54-56 — `() -> Self` — Const generics.
-  `default` function L90-92 — `() -> Self` — Const generics.

#### tests/fixtures/complex_generics/rust/lib.rs

-  `nested` module L6 — `-` — This module contains intentionally complex type signatures
-  `bounds` module L7 — `-` — to test the parser's ability to handle edge cases.
-  `lifetimes` module L8 — `-` — to test the parser's ability to handle edge cases.
-  `associated` module L9 — `-` — to test the parser's ability to handle edge cases.
-  `const_generics` module L10 — `-` — to test the parser's ability to handle edge cases.
-  `pyo3_complex` module L11 — `-` — to test the parser's ability to handle edge cases.

#### tests/fixtures/complex_generics/rust/lifetimes.rs

- pub `longest` function L6-8 — `(a: &'a str, b: &'a str) -> &'a str` — Simple lifetime parameter.
- pub `multi_lifetime` function L11-17 — `( x: &'a str, y: &'b str, z: &'c str, ) -> (&'a str, &'b str, &'c str)` — Multiple distinct lifetimes.
- pub `find_in_slice` function L20-22 — `(slice: &'a [T], predicate: impl Fn(&T) -> bool) -> Option<&'a T>` — Lifetime with generic type parameter.
- pub `with_lifetime_bound` function L25-27 — `(value: &'a T) -> &'a T` — Lifetime bounds on generic parameters.
- pub `Borrowed` struct L30-35 — `{ data: &'a T, name: &'a str }` — Struct with lifetime parameter.
- pub `new` function L39-41 — `(data: &'a T, name: &'a str) -> Self` — Create a new borrowed wrapper.
- pub `get` function L44-46 — `(&self) -> &'a T` — Get the data with the same lifetime.
- pub `for_all_lifetimes` function L53-59 — `(f: F) -> String` — Higher-Ranked Trait Bounds (HRTB).
- pub `complex_hrtb` function L62-71 — `(f: F, items: Vec<T>) -> Vec<String>` — HRTB with multiple parameters.
- pub `Parser` struct L74-77 — `{ input: &'input str, position: usize }` — Lifetime elision edge cases.
- pub `new` function L80-82 — `(input: &'input str) -> Self` — Lifetime parameters and complex borrowing.
- pub `remaining` function L85-87 — `(&self) -> &'input str` — Returns slice with input lifetime (elided in signature).
- pub `split_at` function L90-92 — `(&'a self, pos: usize) -> (&'input str, &'input str)` — Multiple references with different lifetimes.
- pub `covariant` function L96-98 — `(long: &'b str, _short: &'a str) -> &'a str` — Covariant lifetime in return position.

#### tests/fixtures/complex_generics/rust/nested.rs

- pub `process_nested` function L18-33 — `( data: Vec<HashMap<String, Option<T>>>, ) -> Result<Vec<T>, ProcessError>` — Process data with deeply nested generic types.
- pub `deep_nesting` function L36-53 — `( input: HashMap<K, Vec<Option<BTreeMap<String, (V, Vec<V>)>>>>, ) -> Vec<V>` — Even more deeply nested types.
- pub `nested_results` function L56-60 — `( input: Result<Result<Result<T, E1>, E2>, E3>, ) -> Option<T>` — Triple-nested Result types.
- pub `higher_order` function L63-69 — `( f: fn(fn(T) -> U) -> fn(U) -> V, g: fn(T) -> U, x: U, ) -> V` — Nested function pointer types.
- pub `ProcessError` enum L73-78 — `EmptyInput | InvalidData` — Error type for process operations.

#### tests/fixtures/complex_generics/rust/pyo3_complex.rs

- pub `transform_nested` function L19-26 — `( py: Python<'_>, data: HashMap<String, Vec<(i64, Option<String>)>>, config: Opt...` — PyO3 function with complex nested Python types.
- pub `process_py_types` function L30-37 — `( dict: &Bound<'py, PyDict>, list: &Bound<'py, PyList>, tuple: &Bound<'py, PyTup...` — Function with Bound references to Python types.
- pub `GenericWrapper` struct L41-46 — `{ inner: T }` — Generic Python wrapper with complex bounds.
- pub `RefHolder` struct L50-52 — `{ data: String }` — PyO3 class with lifetime in Bound.
- pub `async_transform` function L84-95 — `( py: Python<'py>, items: Vec<HashMap<String, PyObject>>, ) -> PyResult<Bound<'p...` — Async PyO3 function with complex return.
- pub `Processor` struct L99-101 — `{ buffer: Vec<u8> }` — Class with generic pymethods impl block.
- pub `ComplexInput` struct L151-155 — `{ values: Vec<(String, i64)>, metadata: HashMap<String, String>, flags: Vec<bool...` — FromPyObject with complex extraction.
- pub `process_complex_input` function L187-189 — `(input: ComplexInput) -> PyResult<usize>` — Complex PyO3 type signatures.
-  `RefHolder` type L55-80 — `= RefHolder` — Complex PyO3 type signatures.
-  `new` function L57-59 — `(data: String) -> Self` — Complex PyO3 type signatures.
-  `get_slice` function L62-65 — `(&self, py: Python<'py>, start: usize, end: usize) -> PyResult<Bound<'py, pyo3::...` — Method returning borrowed data.
-  `apply_with_kwargs` function L69-79 — `( &self, py: Python<'_>, callback: PyObject, kwargs: Option<&Bound<'_, PyDict>>,...` — Method with complex callback signature.
-  `Processor` type L104-147 — `= Processor` — Complex PyO3 type signatures.
-  `new` function L106-108 — `() -> Self` — Complex PyO3 type signatures.
-  `process_iter` function L111-119 — `(&mut self, data: &Bound<'_, PyList>) -> PyResult<usize>` — Method with impl Trait in argument.
-  `iter_chunks` function L122-133 — `( &self, py: Python<'py>, chunk_size: usize, ) -> PyResult<Bound<'py, PyList>>` — Returns Python iterator over internal data.
-  `sort_items` function L138-146 — `( py: Python<'_>, items: &Bound<'_, PyList>, key: Option<PyObject>, reverse: boo...` — Static method with complex signature.
-  `ComplexInput` type L157-184 — `= ComplexInput` — Complex PyO3 type signatures.
-  `extract_bound` function L158-183 — `(ob: &Bound<'py, PyAny>) -> PyResult<Self>` — Complex PyO3 type signatures.

### tests/fixtures/hybrid_binary/python

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/hybrid_binary/python/helpers.py

- pub `TaskBuilder` class L11-86 — `{ __init__, name, description, depends_on, async_, build }` — Fluent builder for creating tasks.
- pub `__init__` method L26-35 — `def __init__(self, name: str)` — Create a new TaskBuilder.
- pub `description` method L42-52 — `def description(self, desc: str) -> "TaskBuilder"` — Set the task description.
- pub `depends_on` method L54-64 — `def depends_on(self, *task_names: str) -> "TaskBuilder"` — Add dependencies on other tasks.
- pub `async_` method L66-73 — `def async_(self) -> "TaskBuilder"` — Mark this task as async-capable.
- pub `build` method L75-86 — `def build(self) -> Task` — Build the configured Task.
- pub `run_task` function L89-128 — `def run_task( runner: Runner, name: str, handler: Callable, *, description: Opti...` — Convenience function to register and run a task in one call.

### tests/fixtures/hybrid_binary/python/hybrid_binary

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/hybrid_binary/python/hybrid_binary/__init__.py

- pub `get_version` function L17-23 — `def get_version() -> str` — Get the package version string.
- pub `list_submodules` function L26-32 — `def list_submodules() -> List[str]` — List available submodules.

### tests/fixtures/hybrid_binary/python/hybrid_binary/cli

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/hybrid_binary/python/hybrid_binary/cli/__init__.py

- pub `OutputFormat` class L18-29 — `(Enum)` — Output format options for CLI display.
- pub `ColoredOutput` class L32-122 — `{ __init__, red, green, yellow, blue, bold }` — Helper for colored terminal output.
- pub `__init__` method L50-57 — `def __init__(self, stream: Optional[TextIO] = None)` — Create a colored output helper.
- pub `red` method L59-70 — `def red(self, text: str) -> str` — Wrap text in red color.
- pub `green` method L72-83 — `def green(self, text: str) -> str` — Wrap text in green color.
- pub `yellow` method L85-96 — `def yellow(self, text: str) -> str` — Wrap text in yellow color.
- pub `blue` method L98-109 — `def blue(self, text: str) -> str` — Wrap text in blue color.
- pub `bold` method L111-122 — `def bold(self, text: str) -> str` — Make text bold.
- pub `ProgressBar` class L125-179 — `{ __init__, update, render, display, finish }` — A simple progress bar for task execution.
- pub `__init__` method L136-146 — `def __init__(self, total: int, width: int = 40)` — Create a progress bar.
- pub `update` method L148-154 — `def update(self, amount: int = 1) -> None` — Update progress.
- pub `render` method L156-170 — `def render(self) -> str` — Render the progress bar as a string.
- pub `display` method L172-174 — `def display(self) -> None` — Print the progress bar to stdout with carriage return.
- pub `finish` method L176-179 — `def finish(self) -> None` — Complete the progress bar and move to next line.
- pub `format_task_list` function L182-205 — `def format_task_list(tasks: List[str], descriptions: Optional[Dict[str, str]] = ...` — Format a list of tasks for display.

### tests/fixtures/hybrid_binary/python/hybrid_binary/testing

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/hybrid_binary/python/hybrid_binary/testing/__init__.py

- pub `MockTask` class L16-55 — `{ __init__, depends_on, set_async }` — A mock Task for testing without the Rust backend.
- pub `__init__` method L29-39 — `def __init__(self, name: str, description: Optional[str] = None)` — Create a mock task.
- pub `depends_on` method L41-47 — `def depends_on(self, task_name: str) -> None` — Add a dependency.
- pub `set_async` method L49-55 — `def set_async(self, is_async: bool) -> None` — Set async flag.
- pub `MockRunResult` class L58-86 — `{ __init__ }` — A mock RunResult for testing.
- pub `__init__` method L68-86 — `def __init__( self, success: bool = True, tasks_run: int = 1, duration_secs: flo...` — Create a mock result.
- pub `MockRunner` class L89-160 — `{ __init__, register, run, list_tasks, set_next_result }` — A mock Runner for testing task logic.
- pub `__init__` method L99-103 — `def __init__(self)` — Create a new mock runner.
- pub `register` method L105-117 — `def register(self, task: MockTask, handler: Callable) -> None` — Register a task with its handler.
- pub `run` method L119-142 — `def run(self, task_name: str, dry_run: bool = False) -> MockRunResult` — Run a task (mock execution).
- pub `list_tasks` method L144-150 — `def list_tasks(self) -> List[str]` — List registered task names.
- pub `set_next_result` method L152-160 — `def set_next_result(self, result: MockRunResult) -> None` — Set the result for the next run() call.
- pub `assert_task_ran` function L163-174 — `def assert_task_ran(runner: MockRunner, task_name: str) -> None` — Assert that a task was run.
- pub `assert_task_registered` function L177-189 — `def assert_task_registered(runner: MockRunner, task_name: str) -> None` — Assert that a task was registered.

### tests/fixtures/hybrid_binary/rust

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/hybrid_binary/rust/config.rs

- pub `PyRunnerConfig` struct L15-20 — `{ max_parallel: usize, timeout_secs: Option<f64>, retry_count: u32, env_vars: Ha...` — Configuration for a task runner instance.
- pub `PyTaskConfig` struct L96-100 — `{ timeout_secs: Option<f64>, retry_count: Option<u32>, run_async: bool }` — Task-specific configuration overrides.
- pub `load_config` function L150-154 — `(path: &str) -> PyResult<PyRunnerConfig>` — Load configuration from a TOML file.
- pub `register` function L157-164 — `(parent: &Bound<'_, PyModule>) -> PyResult<()>` — Register the config submodule.
-  `PyRunnerConfig` type L23-89 — `= PyRunnerConfig` — for task runner instances.
-  `new` function L32-39 — `(max_parallel: Option<usize>, timeout_secs: Option<f64>, retry_count: Option<u32...` — Create a new runner configuration.
-  `max_parallel` function L43-45 — `(&self) -> usize` — Maximum number of parallel tasks.
-  `set_max_parallel` function L49-51 — `(&mut self, value: usize)` — Set maximum parallel tasks.
-  `timeout_secs` function L55-57 — `(&self) -> Option<f64>` — Timeout in seconds, if set.
-  `retry_count` function L61-63 — `(&self) -> u32` — Number of retry attempts.
-  `set_env` function L70-72 — `(&mut self, key: &str, value: &str)` — Set an environment variable for task execution.
-  `get_env` function L81-83 — `(&self, key: &str) -> Option<String>` — Get an environment variable.
-  `env_vars` function L86-88 — `(&self) -> HashMap<String, String>` — Get all environment variables as a dict.
-  `PyTaskConfig` type L103-137 — `= PyTaskConfig` — for task runner instances.
-  `new` function L112-118 — `(timeout_secs: Option<f64>, retry_count: Option<u32>, run_async: bool) -> Self` — Create task-specific configuration.
-  `timeout_secs` function L122-124 — `(&self) -> Option<f64>` — Task timeout override.
-  `retry_count` function L128-130 — `(&self) -> Option<u32>` — Task retry count override.
-  `run_async` function L134-136 — `(&self) -> bool` — Whether task runs asynchronously.

#### tests/fixtures/hybrid_binary/rust/decorators.rs

- pub `TaskDecorator` struct L22-27 — `{ runner: Py<PyRunner>, name: String, description: Option<String>, depends: Vec<...` — Decorator state that captures registration info.
- pub `task` function L67-91 — `(runner: Py<PyRunner>, kwargs: Option<&Bound<'_, PyDict>>) -> PyResult<TaskDecor...` — Create a task decorator.
- pub `register` function L94-98 — `(m: &Bound<'_, PyModule>) -> PyResult<()>` — Register decorator functions with the module.
-  `TaskDecorator` type L30-44 — `= TaskDecorator` — ```
-  `__call__` function L32-43 — `(&self, py: Python<'_>, func: PyObject) -> PyResult<PyObject>` — Called when the decorator is applied to a function.

#### tests/fixtures/hybrid_binary/rust/internal.rs

- pub `Task` struct L9-14 — `{ name: String, description: Option<String>, dependencies: Vec<String>, is_async...` — A task definition.
- pub `new` function L17-24 — `(name: &str, description: Option<&str>) -> Self` — Internal implementation details (not exposed to Python).
- pub `add_dependency` function L26-28 — `(&mut self, name: &str)` — Internal implementation details (not exposed to Python).
- pub `TaskExecutor` struct L32-35 — `{ max_parallel: usize, tasks: HashMap<String, (Task, PyObject)> }` — Executes tasks with dependency resolution.
- pub `new` function L38-46 — `(max_parallel: usize) -> Result<Self, ExecutorError>` — Internal implementation details (not exposed to Python).
- pub `register` function L48-54 — `(&mut self, task: Task, handler: PyObject) -> Result<(), ExecutorError>` — Internal implementation details (not exposed to Python).
- pub `run` function L56-70 — `(&self, task_name: &str, dry_run: bool) -> Result<RunResult, ExecutorError>` — Internal implementation details (not exposed to Python).
- pub `list_tasks` function L72-74 — `(&self) -> Vec<String>` — Internal implementation details (not exposed to Python).
- pub `RunResult` struct L78-83 — `{ success: bool, tasks_run: usize, duration: Duration, failed: Vec<String> }` — Result of running tasks.
- pub `ExecutorError` enum L87-102 — `InvalidConfig | DuplicateTask | TaskNotFound | CircularDependency | ExecutionFai...` — Errors that can occur during execution.
-  `Task` type L16-29 — `= Task` — Internal implementation details (not exposed to Python).
-  `TaskExecutor` type L37-75 — `= TaskExecutor` — Internal implementation details (not exposed to Python).

#### tests/fixtures/hybrid_binary/rust/lib.rs

- pub `PyTask` struct L21-23 — `{ inner: internal::Task }` — A task that can be executed by the runner.
- pub `PyRunner` struct L74-76 — `{ executor: TaskExecutor }` — The main task runner that executes tasks.
- pub `PyRunResult` struct L134-136 — `{ inner: internal::RunResult }` — Result of a task run.
-  `internal` module L8 — `-` — exposed to Python via PyO3.
-  `decorators` module L9 — `-` — exposed to Python via PyO3.
-  `utils` module L10 — `-` — exposed to Python via PyO3.
-  `config` module L11 — `-` — exposed to Python via PyO3.
-  `PyTask` type L26-67 — `= PyTask` — exposed to Python via PyO3.
-  `new` function L37-41 — `(name: &str, description: Option<&str>) -> Self` — Create a new task.
-  `name` function L45-47 — `(&self) -> &str` — Get the task name.
-  `description` function L51-53 — `(&self) -> Option<&str>` — Get the task description.
-  `depends_on` function L59-61 — `(&mut self, task_name: &str)` — Add a dependency on another task.
-  `set_async` function L64-66 — `(&mut self, is_async: bool)` — Mark this task as async.
-  `PyRunner` type L79-130 — `= PyRunner` — exposed to Python via PyO3.
-  `new` function L86-90 — `(max_parallel: Option<usize>) -> PyResult<Self>` — Create a new task runner.
-  `register` function L100-104 — `(&mut self, task: &PyTask, handler: PyObject) -> PyResult<()>` — Register a task with the runner.
-  `run` function L118-124 — `(&self, task_name: &str, dry_run: bool) -> PyResult<PyRunResult>` — Run a task and all its dependencies.
-  `list_tasks` function L127-129 — `(&self) -> Vec<String>` — List all registered tasks.
-  `PyRunResult` type L139-163 — `= PyRunResult` — exposed to Python via PyO3.
-  `success` function L142-144 — `(&self) -> bool` — Whether all tasks succeeded.
-  `tasks_run` function L148-150 — `(&self) -> usize` — Number of tasks executed.
-  `duration_secs` function L154-156 — `(&self) -> f64` — Total execution time in seconds.
-  `failed` function L160-162 — `(&self) -> Vec<String>` — List of failed task names, if any.
-  `hybrid_binary` function L167-175 — `(m: &Bound<'_, PyModule>) -> PyResult<()>` — Register the module with Python.

#### tests/fixtures/hybrid_binary/rust/utils.rs

- pub `PyTaskLogger` struct L12-15 — `{ level: LogLevel, buffer: Vec<String> }` — A simple logger for task execution.
- pub `LogLevel` enum L20-25 — `Debug | Info | Warning | Error` — Log level for filtering output.
- pub `format_duration` function L84-94 — `(seconds: f64) -> String` — Format a duration in human-readable form.
- pub `register` function L97-104 — `(parent: &Bound<'_, PyModule>) -> PyResult<()>` — Register the utils submodule.
-  `PyTaskLogger` type L28-74 — `= PyTaskLogger` — the task runner system.
-  `new` function L35-40 — `(level: Option<LogLevel>) -> Self` — Create a new task logger.
-  `log` function L47-51 — `(&mut self, level: LogLevel, message: &str)` — Log a message at the specified level.
-  `get_logs` function L54-56 — `(&self) -> Vec<String>` — Get all captured log messages.
-  `clear` function L59-61 — `(&mut self)` — Clear the log buffer.
-  `level` function L65-67 — `(&self) -> LogLevel` — Get the current log level.
-  `set_level` function L71-73 — `(&mut self, level: LogLevel)` — Set the log level.

### tests/fixtures/minimal_hybrid/python/pysnake

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/minimal_hybrid/python/pysnake/__init__.py

- pub `SnakeColor` class L16-22 — `(Enum)` — Colors available for snakes.
- pub `Snake` class L25-65 — `{ __init__, slither, shed }` — A snake entity with configurable properties.
- pub `__init__` method L39-49 — `def __init__(self, name: str, length: int, color: SnakeColor = SnakeColor.GREEN)` — Create a new snake.
- pub `slither` method L51-57 — `def slither(self) -> str` — Make the snake slither forward.
- pub `shed` method L59-65 — `def shed(self) -> bool` — Shed the snake's skin.
- pub `create_snake` function L68-83 — `def create_snake(name: str, length: int = 50) -> Snake` — Factory function to create a snake with defaults.

#### tests/fixtures/minimal_hybrid/python/pysnake/handlers.py

- pub `EventHandler` class L12-42 — `{ __init__, register, emit }` — Handles events emitted by snakes.
- pub `__init__` method L19-21 — `def __init__(self)` — Create a new event handler registry.
- pub `register` method L23-32 — `def register(self, event: str, handler: HandlerFunc) -> None` — Register a handler for an event.
- pub `emit` method L34-42 — `def emit(self, event: str, data: Any) -> None` — Emit an event to all registered handlers.
- pub `on_slither` function L45-59 — `def on_slither(callback: HandlerFunc) -> HandlerFunc` — Decorator to register a slither event handler.

### tests/fixtures/minimal_hybrid/python/pysnake/utils

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/minimal_hybrid/python/pysnake/utils/__init__.py

- pub `Serializer` class L12-48 — `{ __init__, to_json, to_bytes }` — Serializes snake data to various formats.
- pub `__init__` method L19-25 — `def __init__(self, encoding: str = DEFAULT_ENCODING)` — Create a serializer.
- pub `to_json` method L27-37 — `def to_json(self, data: dict) -> str` — Serialize data to JSON string.
- pub `to_bytes` method L39-48 — `def to_bytes(self, data: str) -> bytes` — Convert string to bytes.
- pub `format_length` function L51-61 — `def format_length(length: int, unit: str = "cm") -> str` — Format a length value with units.

#### tests/fixtures/minimal_hybrid/python/pysnake/utils/validation.py

- pub `ValidationError` class L9-26 — `(Exception) { __init__ }` — Raised when validation fails.
- pub `__init__` method L17-26 — `def __init__(self, field: str, message: str)` — Create a validation error.
- pub `validate_length` function L29-55 — `def validate_length(length: int, min_val: int = 1, max_val: int = 1000) -> bool` — Validate a snake length value.
- pub `validate_name` function L58-74 — `def validate_name(name: str) -> bool` — Validate a snake name.

### tests/fixtures/minimal_hybrid/rust/src

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/minimal_hybrid/rust/src/handlers.rs

- pub `ScaleResult` struct L11-21 — `{ input: f64, output: f64, success: bool }` — Result of a scaling operation.
- pub `new` function L33-35 — `(input: f64, output: f64, success: bool) -> Self` — Create a new scale result.
- pub `ratio` function L42-48 — `(&self) -> f64` — Get the scaling ratio (output / input).
- pub `ScaleHandler` struct L53-57 — `{ count: usize }` — Handler that processes scale events.
- pub `new` function L63-65 — `() -> Self` — Create a new scale handler.
- pub `handle` function L72-77 — `(&mut self, result: &ScaleResult)` — Handle a scaling event.
- pub `reset` function L80-82 — `(&mut self)` — Reset the handler state.
- pub `success_result` function L96-98 — `(input: f64, output: f64) -> ScaleResult` — Create a successful scale result.
- pub `register` function L101-108 — `(parent: &Bound<'_, PyModule>) -> PyResult<()>` — Register handlers module with Python.
-  `ScaleResult` type L24-49 — `= ScaleResult` — Demonstrates `rustscale::handlers` namespace with PyO3 bindings.
-  `ScaleHandler` type L60-83 — `= ScaleHandler` — Demonstrates `rustscale::handlers` namespace with PyO3 bindings.

#### tests/fixtures/minimal_hybrid/rust/src/lib.rs

- pub `handlers` module L8 — `-` — and nested module structures.
- pub `internal` module L9 — `-` — and nested module structures.
- pub `MAX_SCALE` variable L12 — `: f64` — Maximum scale factor allowed.
- pub `ScaleMode` enum L17-24 — `Linear | Logarithmic | Exponential` — Available scaling algorithms.
- pub `Scaler` struct L38-45 — `{ factor: f64, mode: ScaleMode }` — A scaler that transforms numeric values.
- pub `new` function L56-58 — `(factor: f64, mode: ScaleMode) -> Self` — Create a new scaler with the given factor and mode.
- pub `scale` function L69-75 — `(&self, value: f64) -> f64` — Scale a value using the configured factor and mode.
- pub `reset` function L78-80 — `(&mut self)` — Reset the scaler to default settings.
- pub `create_linear_scaler` function L102-104 — `(factor: f64) -> Scaler` — Create a linear scaler with the given factor.
-  `Scaler` type L48-81 — `= Scaler` — and nested module structures.
-  `rustscale` function L108-118 — `(m: &Bound<'_, PyModule>) -> PyResult<()>` — Register the module with Python.

### tests/fixtures/minimal_hybrid/rust/src/internal

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/minimal_hybrid/rust/src/internal/mod.rs

- pub `parser` module L6 — `-` — This module contains pure Rust code without PyO3 bindings.
- pub `ScaleConfig` struct L9-14 — `{ precision: f64, max_iterations: usize }` — Internal configuration for scaling operations.
- pub `new` function L18-23 — `() -> Self` — Create a new configuration with defaults.
- pub `high_precision` function L26-31 — `() -> Self` — Create a high-precision configuration.
- pub `InternalError` enum L42-49 — `OutOfRange | ConvergenceFailure | InvalidConfig` — Error types for internal operations.
- pub `clamp` function L78-80 — `(value: f64, min: f64, max: f64) -> f64` — Clamp a value to the given range.
-  `ScaleConfig` type L16-32 — `= ScaleConfig` — It demonstrates `rustscale::internal` namespace.
-  `ScaleConfig` type L34-38 — `impl Default for ScaleConfig` — It demonstrates `rustscale::internal` namespace.
-  `default` function L35-37 — `() -> Self` — It demonstrates `rustscale::internal` namespace.
-  `InternalError` type L51-63 — `= InternalError` — It demonstrates `rustscale::internal` namespace.
-  `fmt` function L52-62 — `(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result` — It demonstrates `rustscale::internal` namespace.
-  `InternalError` type L65 — `= InternalError` — It demonstrates `rustscale::internal` namespace.

#### tests/fixtures/minimal_hybrid/rust/src/internal/parser.rs

- pub `Token` enum L8-21 — `Number | Plus | Star | LParen | RParen | Ident` — Token types for the expression parser.
- pub `Expr` enum L25-34 — `Literal | Variable | Add | Mul` — A parsed scale expression.
- pub `ExprParser` struct L37-40 — `{ tokens: Vec<Token>, position: usize }` — Parser for scale expressions.
- pub `new` function L48-50 — `(tokens: Vec<Token>) -> Self` — Create a new parser from tokens.
- pub `parse` function L57-59 — `(&mut self) -> Option<Expr>` — Parse the tokens into an expression.
- pub `tokenize` function L126-184 — `(input: &str) -> Vec<Token>` — Tokenize a string expression.
-  `ExprParser` type L42-115 — `= ExprParser` — Demonstrates `rustscale::internal::parser` - a deeply nested namespace.
-  `parse_additive` function L61-71 — `(&mut self) -> Option<Expr>` — Demonstrates `rustscale::internal::parser` - a deeply nested namespace.
-  `parse_multiplicative` function L73-83 — `(&mut self) -> Option<Expr>` — Demonstrates `rustscale::internal::parser` - a deeply nested namespace.
-  `parse_primary` function L85-106 — `(&mut self) -> Option<Expr>` — Demonstrates `rustscale::internal::parser` - a deeply nested namespace.
-  `current` function L108-110 — `(&self) -> Option<&Token>` — Demonstrates `rustscale::internal::parser` - a deeply nested namespace.
-  `advance` function L112-114 — `(&mut self)` — Demonstrates `rustscale::internal::parser` - a deeply nested namespace.

### tests/fixtures/pure_python

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/pure_python/async_tasks.py

- pub `AsyncTaskResult` class L20-31 — `(TaskResult)` — Result of an async task execution.
- pub `run_async` function L34-109 — `def run_async( func: Callable[[], Awaitable[T]], *, timeout_seconds: Optional[fl...` — Run an async function with timeout and retry support.
- pub `gather_tasks` function L112-133 — `def gather_tasks( *funcs: Callable[[], Awaitable[Any]], return_exceptions: bool ...` — Run multiple async tasks concurrently.

#### tests/fixtures/pure_python/protocols.py

- pub `Runnable` class L17-30 — `(Protocol) { run }` — Protocol for objects that can be run.
- pub `run` method L28-30 — `def run(self) -> Any` — Execute this runnable and return the result.
- pub `Schedulable` class L34-61 — `(Protocol) { should_run, next_run }` — Protocol for objects that can be scheduled.
- pub `should_run` method L41-50 — `def should_run(self, now: datetime) -> bool` — Check if this should run at the given time.
- pub `next_run` method L52-61 — `def next_run(self, after: datetime) -> datetime` — Calculate the next run time after the given datetime.
- pub `Serializable` class L64-77 — `(Protocol[T_co]) { serialize }` — Protocol for objects that can be serialized.
- pub `serialize` method L71-77 — `def serialize(self) -> T_co` — Serialize this object.
- pub `Deserializable` class L80-97 — `(Protocol[T]) { deserialize }` — Protocol for types that can be deserialized.
- pub `SupportsComparison` class L101-118 — `(Protocol)` — Protocol for objects that support comparison operations.
- pub `__lt__` method L104-106 — `def __lt__(self, other: Any) -> bool` — Less than comparison.
- pub `__le__` method L108-110 — `def __le__(self, other: Any) -> bool` — Less than or equal comparison.
- pub `__gt__` method L112-114 — `def __gt__(self, other: Any) -> bool` — Greater than comparison.
- pub `__ge__` method L116-118 — `def __ge__(self, other: Any) -> bool` — Greater than or equal comparison.

#### tests/fixtures/pure_python/schedule.py

- pub `Schedule` class L11-42 — `(ABC) { next_run, should_run }` — Base class for task schedules.
- pub `should_run` method L30-42 — `def should_run(self, now: datetime, last_run: Optional[datetime]) -> bool` — Check if the task should run now.
- pub `IntervalSchedule` class L46-57 — `(Schedule) { next_run }` — Run a task at fixed intervals.
- pub `next_run` method L55-57 — `def next_run(self, after: datetime) -> datetime` — Get the next run time.
- pub `CronSchedule` class L61-89 — `(Schedule) { next_run }` — Run a task according to a cron expression.
- pub `__post_init__` method L77-83 — `def __post_init__(self)` — Parse and validate the cron expression.
- pub `next_run` method L85-89 — `def next_run(self, after: datetime) -> datetime` — Calculate next run time from cron expression.
- pub `DailySchedule` class L93-112 — `(Schedule) { next_run }` — Run a task at a specific time each day.
- pub `next_run` method L102-112 — `def next_run(self, after: datetime) -> datetime` — Get the next run time.
- pub `every` function L115-145 — `def every( *, seconds: int = 0, minutes: int = 0, hours: int = 0, days: int = 0,...` — Create an interval schedule.
- pub `cron` function L148-162 — `def cron(expression: str) -> CronSchedule` — Create a cron schedule.
- pub `at` function L165-180 — `def at(hour: int, minute: int = 0, second: int = 0) -> DailySchedule` — Create a daily schedule at a specific time.

#### tests/fixtures/pure_python/scheduler.py

- pub `Scheduler` class L17-171 — `{ __init__, tasks, running, task, run, stop, run_task }` — A task scheduler that runs tasks on configured schedules.
- pub `__init__` method L35-44 — `def __init__(self, timezone: Optional[str] = None)` — Initialize a new scheduler.
- pub `task` method L56-101 — `def task( self, schedule: Schedule, *, name: Optional[str] = None, max_retries: ...` — Decorator to register a function as a scheduled task.
- pub `run` method L103-126 — `def run(self, *, blocking: bool = True) -> None` — Start the scheduler.
- pub `stop` method L128-131 — `def stop(self) -> None` — Stop the scheduler gracefully.
- pub `run_task` method L133-148 — `def run_task(self, name: str) -> TaskResult` — Manually run a task by name.
-  `_run_loop` method L150-166 — `def _run_loop(self) -> None` — Main scheduler loop.
-  `_setup_signal_handlers` method L168-171 — `def _setup_signal_handlers(self) -> None` — Set up signal handlers for graceful shutdown.

#### tests/fixtures/pure_python/task.py

- pub `TaskStatus` class L15-34 — `(Enum)` — Status of a task execution.
- pub `TaskResult` class L38-62 — `{ success }` — Result of a task execution.
- pub `Task` class L66-174 — `{ should_run, run }` — A scheduled task.
- pub `should_run` method L90-99 — `def should_run(self, now: datetime) -> bool` — Check if this task should run now.
- pub `run` method L101-159 — `def run(self) -> TaskResult` — Execute the task.
-  `_execute_with_timeout` method L161-174 — `def _execute_with_timeout(self) -> Any` — Execute the function with optional timeout.

### tests/fixtures/pure_python/utils

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/pure_python/utils/logging.py

- pub `LogLevel` class L14-33 — `(IntEnum)` — Log level constants.
- pub `get_logger` function L43-77 — `def get_logger( name: str, *, level: LogLevel = LogLevel.INFO, stream: TextIO | ...` — Get or create a configured logger.
- pub `StructuredLogger` class L80-151 — `{ __init__, with_context, debug, info, warning, error, critical }` — A logger that outputs structured log data.
- pub `__init__` method L91-106 — `def __init__( self, name: str, *, level: LogLevel = LogLevel.INFO, **context: An...` — Create a new structured logger.
- pub `with_context` method L108-123 — `def with_context(self, **extra: Any) -> StructuredLogger` — Create a child logger with additional context.
- pub `debug` method L133-135 — `def debug(self, msg: str, **kwargs: Any) -> None` — Log a debug message.
- pub `info` method L137-139 — `def info(self, msg: str, **kwargs: Any) -> None` — Log an info message.
- pub `warning` method L141-143 — `def warning(self, msg: str, **kwargs: Any) -> None` — Log a warning message.
- pub `error` method L145-147 — `def error(self, msg: str, **kwargs: Any) -> None` — Log an error message.
- pub `critical` method L149-151 — `def critical(self, msg: str, **kwargs: Any) -> None` — Log a critical message.
-  `_format_message` method L125-131 — `def _format_message(self, msg: str, **kwargs: Any) -> str` — Format message with context.

#### tests/fixtures/pure_python/utils/timing.py

- pub `TimeUnit` class L23-42 — `(Enum)` — Units of time for duration formatting.
- pub `TimingResult` class L46-60 — `{ elapsed_ms }` — Result of a timing measurement.
- pub `timed` function L83-108 — `def timed(func: Callable[..., T]) -> Callable[..., tuple[T, float]]` — Decorator to measure function execution time.
- pub `format_duration` function L111-157 — `def format_duration( seconds: float, *, unit: TimeUnit | None = None, precision:...` — Format a duration as a human-readable string.

### tests/fixtures/pure_rust/src

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/pure_rust/src/async_loader.rs

- pub `AsyncLoader` interface L16-26 — `{ fn load(), fn source_description() }` — Trait for async configuration loading.
- pub `AsyncFileLoader` struct L29-31 — `{ path: std::path::PathBuf }` — Async file loader that reads configuration from the filesystem.
- pub `new` function L39-43 — `(path: impl AsRef<Path>) -> Self` — Create a new async file loader.
- pub `HttpLoader` struct L62-65 — `{ url: String, timeout_secs: u64 }` — Loader that fetches configuration from a remote URL.
- pub `new` function L73-78 — `(url: impl Into<String>) -> Self` — Create a new HTTP configuration loader.
- pub `with_timeout` function L85-88 — `(mut self, secs: u64) -> Self` — Set the request timeout.
- pub `load_with_fallback` function L119-130 — `(loaders: &[&dyn AsyncLoader]) -> Result<Config, LoadError>` — Load configuration from multiple async sources with fallback.
-  `AsyncFileLoader` type L33-44 — `= AsyncFileLoader` — with async runtimes like Tokio.
-  `AsyncFileLoader` type L46-59 — `impl AsyncLoader for AsyncFileLoader` — with async runtimes like Tokio.
-  `load` function L47-54 — `(&self) -> Pin<Box<dyn Future<Output = Result<Config, LoadError>> + Send + '_>>` — with async runtimes like Tokio.
-  `source_description` function L56-58 — `(&self) -> String` — with async runtimes like Tokio.
-  `HttpLoader` type L67-89 — `= HttpLoader` — with async runtimes like Tokio.
-  `HttpLoader` type L91-106 — `impl AsyncLoader for HttpLoader` — with async runtimes like Tokio.
-  `load` function L92-101 — `(&self) -> Pin<Box<dyn Future<Output = Result<Config, LoadError>> + Send + '_>>` — with async runtimes like Tokio.
-  `source_description` function L103-105 — `(&self) -> String` — with async runtimes like Tokio.

#### tests/fixtures/pure_rust/src/config.rs

- pub `Value` enum L7-22 — `Null | Bool | Int | Float | String | Array | Map` — A configuration value.
- pub `is_null` function L26-28 — `(&self) -> bool` — Check if this value is null.
- pub `as_bool` function L31-36 — `(&self) -> Option<bool>` — Try to get as a boolean.
- pub `as_str` function L39-44 — `(&self) -> Option<&str>` — Try to get as a string.
- pub `as_int` function L47-52 — `(&self) -> Option<i64>` — Try to get as an integer.
- pub `Config` struct L57-60 — `{ values: HashMap<String, Value>, source: Option<String> }` — A loaded configuration.
- pub `new` function L64-69 — `() -> Self` — Create an empty configuration.
- pub `get` function L74-77 — `(&self, key: &str) -> Option<&Value>` — Get a value by key.
- pub `require_str` function L84-92 — `(&self, key: &str) -> Result<&str, ConfigError>` — Get a required string value.
- pub `set` function L95-97 — `(&mut self, key: impl Into<String>, value: Value)` — Set a value.
- pub `source` function L100-102 — `(&self) -> Option<&str>` — Get the source identifier.
- pub `ConfigError` enum L113-119 — `MissingKey | TypeMismatch` — Error type for config operations.
- pub `ConfigBuilder` struct L122-124 — `{ config: Config }` — Builder for constructing configurations programmatically.
- pub `new` function L128-132 — `() -> Self` — Create a new builder.
- pub `set_str` function L135-138 — `(mut self, key: &str, value: &str) -> Self` — Set a string value.
- pub `set_int` function L141-144 — `(mut self, key: &str, value: i64) -> Self` — Set an integer value.
- pub `set_bool` function L147-150 — `(mut self, key: &str, value: bool) -> Self` — Set a boolean value.
- pub `build` function L153-155 — `(self) -> Config` — Build the configuration.
-  `Value` type L24-53 — `= Value` — Core configuration types.
-  `Config` type L62-103 — `= Config` — Core configuration types.
-  `Config` type L105-109 — `impl Default for Config` — Core configuration types.
-  `default` function L106-108 — `() -> Self` — Core configuration types.
-  `ConfigBuilder` type L126-156 — `= ConfigBuilder` — Core configuration types.
-  `ConfigBuilder` type L158-162 — `impl Default for ConfigBuilder` — Core configuration types.
-  `default` function L159-161 — `() -> Self` — Core configuration types.

#### tests/fixtures/pure_rust/src/error.rs

- pub `LoadError` enum L5-17 — `Io | UnknownFormat | ParseError` — Errors that can occur when loading configuration.
- pub `ValidationError` enum L21-33 — `MissingField | InvalidValue | Multiple` — Errors that can occur during validation.

#### tests/fixtures/pure_rust/src/lib.rs

- pub `transform` module L33 — `-` — ```
-  `config` module L28 — `-` — Provides type-safe configuration loading and validation
-  `loader` module L29 — `-` — ```
-  `error` module L30 — `-` — ```
-  `validate` module L31 — `-` — ```
-  `async_loader` module L32 — `-` — ```

#### tests/fixtures/pure_rust/src/loader.rs

- pub `Loader` interface L8-14 — `{ fn load(), fn source_description() }` — Trait for loading configuration from various sources.
- pub `FileLoader` struct L17-20 — `{ path: std::path::PathBuf, format: FileFormat }` — Loads configuration from a file.
- pub `FileFormat` enum L24-31 — `Json | Toml | Yaml` — Supported file formats.
- pub `new` function L37-44 — `(path: impl AsRef<Path>) -> Result<Self, LoadError>` — Create a new file loader.
- pub `with_format` function L47-52 — `(path: impl AsRef<Path>, format: FileFormat) -> Self` — Create a file loader with explicit format.
- pub `EnvLoader` struct L80-83 — `{ prefix: Option<String>, separator: String }` — Loads configuration from environment variables.
- pub `new` function L87-92 — `() -> Self` — Create a new environment loader.
- pub `with_prefix` function L97-100 — `(mut self, prefix: &str) -> Self` — Only load variables with this prefix.
- pub `with_separator` function L105-108 — `(mut self, sep: &str) -> Self` — Use a custom separator for nested keys.
-  `FileLoader` type L33-62 — `= FileLoader` — Configuration loaders.
-  `detect_format` function L54-61 — `(path: &Path) -> Result<FileFormat, LoadError>` — Configuration loaders.
-  `FileLoader` type L64-77 — `impl Loader for FileLoader` — Configuration loaders.
-  `load` function L65-72 — `(&self) -> Result<Config, LoadError>` — Configuration loaders.
-  `source_description` function L74-76 — `(&self) -> String` — Configuration loaders.
-  `EnvLoader` type L85-109 — `= EnvLoader` — Configuration loaders.
-  `EnvLoader` type L111-115 — `impl Default for EnvLoader` — Configuration loaders.
-  `default` function L112-114 — `() -> Self` — Configuration loaders.
-  `EnvLoader` type L117-149 — `impl Loader for EnvLoader` — Configuration loaders.
-  `load` function L118-141 — `(&self) -> Result<Config, LoadError>` — Configuration loaders.
-  `source_description` function L143-148 — `(&self) -> String` — Configuration loaders.

#### tests/fixtures/pure_rust/src/validate.rs

- pub `Rule` interface L7-15 — `{ fn validate(), fn description() }` — A validation rule.
- pub `Validator` struct L18-20 — `{ rules: Vec<Box<dyn Rule>> }` — A configuration validator that applies multiple rules.
- pub `new` function L24-26 — `() -> Self` — Create a new validator with no rules.
- pub `rule` function L29-32 — `(mut self, rule: impl Rule + 'static) -> Self` — Add a rule to this validator.
- pub `required` function L35-37 — `(self, field: &str) -> Self` — Add a required field rule.
- pub `validate` function L42-56 — `(&self, config: &Config) -> Result<(), ValidationError>` — Validate a configuration against all rules.
- pub `FieldRule` struct L82-86 — `{ field: String, predicate: F, message: String }` — Rule that validates a field matches a predicate.
- pub `new` function L93-99 — `(field: &str, predicate: F, message: &str) -> Self` — Create a new field rule.
-  `Validator` type L22-57 — `= Validator` — Configuration validation.
-  `Validator` type L59-63 — `impl Default for Validator` — Configuration validation.
-  `default` function L60-62 — `() -> Self` — Configuration validation.
-  `RequiredField` struct L66 — `-` — Rule that requires a field to be present and non-null.
-  `RequiredField` type L68-79 — `impl Rule for RequiredField` — Configuration validation.
-  `validate` function L69-74 — `(&self, config: &Config) -> Result<(), ValidationError>` — Configuration validation.
-  `description` function L76-78 — `(&self) -> &str` — Configuration validation.
-  `validate` function L106-115 — `(&self, config: &Config) -> Result<(), ValidationError>` — Configuration validation.
-  `description` function L117-119 — `(&self) -> &str` — Configuration validation.

### tests/fixtures/pure_rust/src/transform

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/pure_rust/src/transform/filters.rs

- pub `Filter` interface L8-22 — `{ fn accept(), fn description() }` — A filter that can accept or reject values.
- pub `FilterFn` struct L29-35 — `{ predicate: F, description: String }` — A filter implemented as a function.
- pub `new` function L47-52 — `(predicate: F, description: impl Into<String>) -> Self` — Create a new function-based filter.
- pub `StringFilter` struct L69-71 — `{ kind: StringFilterKind }` — Filters for string values.
- pub `min_length` function L84-88 — `(len: usize) -> Self` — Create a filter requiring minimum string length.
- pub `max_length` function L91-95 — `(len: usize) -> Self` — Create a filter requiring maximum string length.
- pub `contains` function L98-102 — `(substring: impl Into<String>) -> Self` — Create a filter requiring the string to contain a substring.
- pub `starts_with` function L105-109 — `(prefix: impl Into<String>) -> Self` — Create a filter requiring the string to start with a prefix.
- pub `ends_with` function L112-116 — `(suffix: impl Into<String>) -> Self` — Create a filter requiring the string to end with a suffix.
- pub `matches` function L123-127 — `(pattern: &str) -> Self` — Create a filter requiring the string to match a regex pattern.
- pub `AllOf` struct L160-162 — `{ filters: Vec<Box<dyn Filter>> }` — Combine multiple filters with AND logic.
- pub `new` function L166-168 — `(filters: Vec<Box<dyn Filter>>) -> Self` — Create a new composite filter.
- pub `AnyOf` struct L182-184 — `{ filters: Vec<Box<dyn Filter>> }` — Combine multiple filters with OR logic.
- pub `new` function L188-190 — `(filters: Vec<Box<dyn Filter>>) -> Self` — Create a new composite filter.
- pub `Regex` struct L205 — `-` — Provides traits and implementations for filtering configuration values.
- pub `new` function L208-210 — `(pattern: &str) -> Result<Self, ()>` — Provides traits and implementations for filtering configuration values.
- pub `is_match` function L212-215 — `(&self, _text: &str) -> bool` — Provides traits and implementations for filtering configuration values.
-  `accept` function L59-61 — `(&self, value: &Value) -> bool` — Provides traits and implementations for filtering configuration values.
-  `description` function L63-65 — `(&self) -> &str` — Provides traits and implementations for filtering configuration values.
-  `StringFilterKind` enum L73-80 — `MinLength | MaxLength | Contains | StartsWith | EndsWith | Matches` — Provides traits and implementations for filtering configuration values.
-  `StringFilter` type L82-128 — `= StringFilter` — Provides traits and implementations for filtering configuration values.
-  `StringFilter` type L130-157 — `impl Filter for StringFilter` — Provides traits and implementations for filtering configuration values.
-  `accept` function L131-145 — `(&self, value: &Value) -> bool` — Provides traits and implementations for filtering configuration values.
-  `description` function L147-156 — `(&self) -> &str` — Provides traits and implementations for filtering configuration values.
-  `AllOf` type L164-169 — `= AllOf` — Provides traits and implementations for filtering configuration values.
-  `AllOf` type L171-179 — `impl Filter for AllOf` — Provides traits and implementations for filtering configuration values.
-  `accept` function L172-174 — `(&self, value: &Value) -> bool` — Provides traits and implementations for filtering configuration values.
-  `description` function L176-178 — `(&self) -> &str` — Provides traits and implementations for filtering configuration values.
-  `AnyOf` type L186-191 — `= AnyOf` — Provides traits and implementations for filtering configuration values.
-  `AnyOf` type L193-201 — `impl Filter for AnyOf` — Provides traits and implementations for filtering configuration values.
-  `accept` function L194-196 — `(&self, value: &Value) -> bool` — Provides traits and implementations for filtering configuration values.
-  `description` function L198-200 — `(&self) -> &str` — Provides traits and implementations for filtering configuration values.
-  `regex` module L204-217 — `-` — Provides traits and implementations for filtering configuration values.
-  `Regex` type L207-216 — `= Regex` — Provides traits and implementations for filtering configuration values.

#### tests/fixtures/pure_rust/src/transform/mod.rs

- pub `TransformError` enum L16-33 — `TypeMismatch | Missing | Custom` — Errors that can occur during transformation.
- pub `TransformResult` type L36 — `= Result<T, TransformError>` — Result type for transform operations.
- pub `FixedBuffer` struct L46-49 — `{ data: [Option<T>; N], len: usize }` — A buffer with a compile-time fixed capacity.
- pub `new` function L53-61 — `() -> Self` — Create a new empty buffer.
- pub `push` function L68-75 — `(&mut self, value: T) -> Result<(), T>` — Push a value onto the buffer.
- pub `len` function L78-80 — `(&self) -> usize` — Get the number of elements in the buffer.
- pub `is_empty` function L83-85 — `(&self) -> bool` — Check if the buffer is empty.
- pub `capacity` function L88-90 — `(&self) -> usize` — Get the buffer capacity.
- pub `convert` function L106-111 — `(value: T, converter: F) -> U` — Convert a value using a generic converter.
- pub `map_value` function L114-119 — `(value: Option<Value>, f: F) -> Option<Value>` — Apply a transformation to a Value, if present.
-  `pipeline` module L6 — `-` — This module provides utilities for transforming configuration
-  `filters` module L7 — `-` — values between formats and applying modifications.
-  `default` function L94-96 — `() -> Self` — values between formats and applying modifications.

#### tests/fixtures/pure_rust/src/transform/pipeline.rs

- pub `Transform` interface L7-21 — `{ fn apply(), fn name() }` — A transformation that can be applied to a value.
- pub `TransformPipeline` struct L35-38 — `{ transforms: Vec<Box<dyn Transform>>, name: String }` — A pipeline of transformations applied in sequence.
- pub `new` function L42-47 — `() -> Self` — Create a new empty pipeline.
- pub `named` function L50-55 — `(name: impl Into<String>) -> Self` — Create a pipeline with a custom name.
- pub `add` function L60-63 — `(mut self, transform: impl Transform + 'static) -> Self` — Add a transform to the pipeline.
- pub `execute` function L74-79 — `(&self, mut value: Value) -> TransformResult<Value>` — Execute all transforms in the pipeline.
- pub `len` function L82-84 — `(&self) -> usize` — Get the number of transforms in the pipeline.
- pub `is_empty` function L87-89 — `(&self) -> bool` — Check if the pipeline is empty.
- pub `UppercaseTransform` struct L99 — `-` — Transform that converts string values to uppercase.
- pub `LowercaseTransform` struct L118 — `-` — Transform that converts string values to lowercase.
- pub `TrimTransform` struct L137 — `-` — Transform that trims whitespace from string values.
-  `TransformPipeline` type L40-90 — `= TransformPipeline` — Transform pipeline for chaining transformations.
-  `TransformPipeline` type L92-96 — `impl Default for TransformPipeline` — Transform pipeline for chaining transformations.
-  `default` function L93-95 — `() -> Self` — Transform pipeline for chaining transformations.
-  `UppercaseTransform` type L101-115 — `impl Transform for UppercaseTransform` — Transform pipeline for chaining transformations.
-  `apply` function L102-110 — `(&self, value: Value) -> TransformResult<Value>` — Transform pipeline for chaining transformations.
-  `name` function L112-114 — `(&self) -> &str` — Transform pipeline for chaining transformations.
-  `LowercaseTransform` type L120-134 — `impl Transform for LowercaseTransform` — Transform pipeline for chaining transformations.
-  `apply` function L121-129 — `(&self, value: Value) -> TransformResult<Value>` — Transform pipeline for chaining transformations.
-  `name` function L131-133 — `(&self) -> &str` — Transform pipeline for chaining transformations.
-  `TrimTransform` type L139-153 — `impl Transform for TrimTransform` — Transform pipeline for chaining transformations.
-  `apply` function L140-148 — `(&self, value: Value) -> TransformResult<Value>` — Transform pipeline for chaining transformations.
-  `name` function L150-152 — `(&self) -> &str` — Transform pipeline for chaining transformations.
-  `type_name` function L155-165 — `(value: &Value) -> &'static str` — Transform pipeline for chaining transformations.

### tests/fixtures/separate_bindings/bindings/src

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/separate_bindings/bindings/src/lib.rs

- pub `PyPipeline` struct L37-40 — `{ inner: CorePipeline, py_stages: Vec<(String, PyObject)> }` — A data processing pipeline.
- pub `PyPipelineResult` struct L114-121 — `{ output: PyDataBatch, stages_run: usize, duration_secs: f64 }` — Result of a pipeline run.
- pub `PyDataBatch` struct L139-141 — `{ inner: CoreBatch }` — A batch of data rows.
-  `PyPipeline` type L43-110 — `= PyPipeline` — functionality to Python users.
-  `new` function L49-54 — `(name: &str) -> Self` — Create a new pipeline.
-  `name` function L58-60 — `(&self) -> &str` — Get the pipeline name.
-  `add_stage` function L70-73 — `(&mut self, name: &str, processor: PyObject) -> PyResult<()>` — Add a processing stage.
-  `stage_count` function L76-78 — `(&self) -> usize` — Get the number of stages.
-  `run` function L90-109 — `(&self, py: Python<'_>, input: &PyDataBatch) -> PyResult<PyPipelineResult>` — Run the pipeline.
-  `PyPipelineResult` type L124-130 — `= PyPipelineResult` — functionality to Python users.
-  `rows_out` function L127-129 — `(&self) -> usize` — Number of rows in the output.
-  `PyDataBatch` type L144-211 — `= PyDataBatch` — functionality to Python users.
-  `new` function L147-151 — `() -> Self` — Create an empty batch.
-  `from_dicts` function L169-182 — `(rows: Vec<HashMap<String, PyObject>>, py: Python<'_>) -> PyResult<Self>` — Create a batch from a list of dictionaries.
-  `__len__` function L185-187 — `(&self) -> usize` — Get the number of rows.
-  `len` function L190-192 — `(&self) -> usize` — Get the number of rows.
-  `is_empty` function L195-197 — `(&self) -> bool` — Check if batch is empty.
-  `to_dicts` function L200-210 — `(&self, py: Python<'_>) -> PyResult<Vec<HashMap<String, PyObject>>>` — Convert to a list of dictionaries.
-  `py_to_value` function L215-229 — `(py: Python<'_>, obj: &PyObject) -> PyResult<CoreValue>` — functionality to Python users.
-  `value_to_py` function L231-240 — `(py: Python<'_>, value: &CoreValue) -> PyResult<PyObject>` — functionality to Python users.
-  `separate_bindings` function L244-249 — `(m: &Bound<'_, PyModule>) -> PyResult<()>` — Python module definition.

### tests/fixtures/separate_bindings/core/src

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/separate_bindings/core/src/batch.rs

- pub `DataBatch` struct L10-13 — `{ rows: Vec<Row>, metadata: BatchMetadata }` — A batch of data flowing through the pipeline.
- pub `Row` type L16 — `= HashMap<String, Value>` — A single row of data.
- pub `Value` enum L20-29 — `Null | Bool | Int | Float | String | Bytes | List | Map` — A data value that can appear in a row.
- pub `BatchMetadata` struct L33-40 — `{ source: Option<String>, schema_version: Option<String>, attributes: HashMap<St...` — Metadata about a batch.
- pub `new` function L44-46 — `() -> Self` — Create an empty batch.
- pub `from_rows` function L49-54 — `(rows: Vec<Row>) -> Self` — Create a batch from rows.
- pub `len` function L57-59 — `(&self) -> usize` — Get the number of rows.
- pub `is_empty` function L62-64 — `(&self) -> bool` — Check if the batch is empty.
- pub `rows` function L67-69 — `(&self) -> &[Row]` — Get a reference to the rows.
- pub `metadata` function L72-74 — `(&self) -> &BatchMetadata` — Get the metadata.
- pub `with_metadata` function L77-80 — `(mut self, metadata: BatchMetadata) -> Self` — Set metadata.
- pub `map` function L83-91 — `(self, f: F) -> Self` — Map over all rows.
- pub `filter` function L94-102 — `(self, predicate: F) -> Self` — Filter rows.
-  `DataBatch` type L42-103 — `= DataBatch` — Data batch types.

#### tests/fixtures/separate_bindings/core/src/error.rs

- pub `PipelineError` enum L7-24 — `StageFailedAfterRetries | Timeout | InvalidConfig` — Errors that can occur during pipeline execution.
- pub `StageError` enum L28-44 — `ValidationError | TransformError | IoError | ProcessingError` — Errors that can occur during stage execution.

#### tests/fixtures/separate_bindings/core/src/lib.rs

- pub `stream` module L32 — `-` — ```
-  `pipeline` module L28 — `-` — This crate provides a type-safe, composable pipeline system
-  `stage` module L29 — `-` — ```
-  `batch` module L30 — `-` — ```
-  `error` module L31 — `-` — ```

#### tests/fixtures/separate_bindings/core/src/pipeline.rs

- pub `PipelineConfig` struct L10-17 — `{ max_retries: usize, continue_on_error: bool, timeout: Option<Duration> }` — Configuration options for pipeline execution.
- pub `Pipeline` struct L34-38 — `{ name: String, stages: Vec<Stage>, config: PipelineConfig }` — A data processing pipeline.
- pub `new` function L42-48 — `(name: impl Into<String>) -> Self` — Create a new pipeline with the given name.
- pub `name` function L51-53 — `(&self) -> &str` — Get the pipeline name.
- pub `stage` function L58-61 — `(mut self, stage: Stage) -> Self` — Add a stage to the pipeline.
- pub `config` function L64-67 — `(mut self, config: PipelineConfig) -> Self` — Configure the pipeline.
- pub `stage_count` function L70-72 — `(&self) -> usize` — Get the number of stages.
- pub `run` function L77-93 — `(&self, input: DataBatch) -> Result<PipelineResult, PipelineError>` — Run the pipeline with the given input batch.
- pub `PipelineResult` struct L119-126 — `{ output: DataBatch, stages_run: usize, duration: Duration }` — Result of a pipeline execution.
-  `PipelineConfig` type L19-27 — `impl Default for PipelineConfig` — Pipeline orchestration.
-  `default` function L20-26 — `() -> Self` — Pipeline orchestration.
-  `Pipeline` type L40-115 — `= Pipeline` — Pipeline orchestration.
-  `run_stage` function L95-114 — `(&self, stage: &Stage, input: DataBatch) -> Result<DataBatch, PipelineError>` — Pipeline orchestration.

#### tests/fixtures/separate_bindings/core/src/stage.rs

- pub `Stage` struct L10-13 — `{ name: String, processor: Box<dyn Fn(DataBatch) -> Result<DataBatch, StageError...` — A processing stage in a pipeline.
- pub `new` function L17-25 — `(name: impl Into<String>, processor: F) -> Self` — Create a new stage with the given name and processor function.
- pub `name` function L28-30 — `(&self) -> &str` — Get the stage name.
- pub `process` function L33-35 — `(&self, input: DataBatch) -> Result<DataBatch, StageError>` — Process a data batch.
- pub `StageBuilder` struct L39-43 — `{ name: String, retryable: bool, timeout_ms: Option<u64> }` — Builder for creating stages with additional configuration.
- pub `new` function L47-53 — `(name: impl Into<String>) -> Self` — Start building a new stage.
- pub `no_retry` function L56-59 — `(mut self) -> Self` — Mark this stage as non-retryable.
- pub `timeout_ms` function L62-65 — `(mut self, ms: u64) -> Self` — Set a timeout for this stage in milliseconds.
- pub `build` function L68-75 — `(self, processor: F) -> Stage` — Build the stage with the given processor.
-  `Stage` type L15-36 — `= Stage` — Pipeline stages.
-  `StageBuilder` type L45-76 — `= StageBuilder` — Pipeline stages.

#### tests/fixtures/separate_bindings/core/src/stream.rs

- pub `AsyncSource` interface L16-33 — `{ fn read_batch(), fn estimated_remaining() }` — An async data source that yields batches.
- pub `AsyncSink` interface L36-51 — `{ fn write_batch(), fn flush(), fn close() }` — An async data sink that consumes batches.
- pub `AsyncProcessor` interface L54-71 — `{ fn process(), fn name() }` — An async processor that transforms batches.
- pub `StreamingPipeline` struct L74-83 — `{ source: S, sink: K, processors: Vec<Box<dyn AsyncProcessor>>, batch_size: usiz...` — A streaming pipeline that processes batches from source to sink.
- pub `new` function L96-103 — `(source: S, sink: K) -> Self` — Create a new streaming pipeline.
- pub `batch_size` function L106-109 — `(mut self, size: usize) -> Self` — Set the batch size for processing.
- pub `add_processor` function L112-115 — `(mut self, processor: impl AsyncProcessor + 'static) -> Self` — Add a processor to the pipeline.
- pub `processor_count` function L118-120 — `(&self) -> usize` — Get the number of processors.
- pub `StreamStats` struct L125-134 — `{ batches_read: usize, batches_written: usize, rows_processed: usize, errors: us...` — Statistics from a streaming run.
- pub `new` function L138-140 — `() -> Self` — Create empty statistics.
- pub `merge` function L143-148 — `(&mut self, other: &StreamStats)` — Merge statistics from another run.
- pub `BatchCollector` struct L153-156 — `{ batches: Vec<DataBatch>, max_batches: Option<usize> }` — A batch collector that accumulates batches.
- pub `new` function L160-162 — `() -> Self` — Create a new collector with no limit.
- pub `with_limit` function L165-170 — `(max: usize) -> Self` — Create a collector with a maximum batch count.
- pub `push` function L175-183 — `(&mut self, batch: DataBatch) -> bool` — Add a batch to the collector.
- pub `into_batches` function L186-188 — `(self) -> Vec<DataBatch>` — Get the collected batches.
- pub `len` function L191-193 — `(&self) -> usize` — Get the number of collected batches.
- pub `is_empty` function L196-198 — `(&self) -> bool` — Check if the collector is empty.
-  `estimated_remaining` function L30-32 — `(&self) -> Option<usize>` — Get an estimated count of remaining batches.
-  `StreamStats` type L136-149 — `= StreamStats` — data batches in an async context.
-  `BatchCollector` type L158-199 — `= BatchCollector` — data batches in an async context.

### tests/fixtures/separate_bindings/python

> *Semantic summary to be generated by AI agent.*

#### tests/fixtures/separate_bindings/python/async_helpers.py

- pub `AsyncPipelineResult` class L29-52 — `{ success, duration_secs }` — Result of an async pipeline run.
- pub `run_async` function L55-113 — `def run_async( pipeline: Pipeline, batch: DataBatch, *, timeout: Optional[float]...` — Run a pipeline asynchronously.
- pub `run_concurrent` function L116-144 — `def run_concurrent( pipeline: Pipeline, batches: List[DataBatch], *, max_concurr...` — Run a pipeline on multiple batches concurrently.
- pub `StreamContext` class L148-159 — `(Generic[T])` — Context for async stream processing.
- pub `stream_batches` function L162-189 — `def stream_batches( source: Callable[[], Awaitable[Optional[DataBatch]]], *, max...` — Create an async iterator of batches from a source.
- pub `collect_results` function L192-206 — `def collect_results( results: AsyncIterator[AsyncPipelineResult], ) -> List[Asyn...` — Collect async results into a list.

#### tests/fixtures/separate_bindings/python/helpers.py

- pub `PipelineBuilder` class L13-110 — `{ __init__, name, stage, extract, transform, load, build }` — Fluent builder for creating pipelines.
- pub `__init__` method L29-36 — `def __init__(self, name: str)` — Create a new pipeline builder.
- pub `stage` method L43-56 — `def stage( self, name: str, processor: Callable[[DataBatch], DataBatch] ) -> "Pi...` — Add a generic stage.
- pub `extract` method L58-71 — `def extract(self, processor: Callable[[DataBatch], DataBatch]) -> "PipelineBuild...` — Add an extraction stage.
- pub `transform` method L73-86 — `def transform( self, processor: Callable[[DataBatch], DataBatch] ) -> "PipelineB...` — Add a transformation stage.
- pub `load` method L88-99 — `def load(self, processor: Callable[[DataBatch], DataBatch]) -> "PipelineBuilder"` — Add a load stage.
- pub `build` method L101-110 — `def build(self) -> Pipeline` — Build the configured pipeline.
- pub `batch_from_iter` function L113-142 — `def batch_from_iter( items: Iterator[Dict[str, Any]], chunk_size: int = 1000 ) -...` — Create DataBatches from an iterator of dictionaries.
- pub `run_parallel` function L145-179 — `def run_parallel( pipeline: Pipeline, batches: List[DataBatch], max_workers: Opt...` — Run a pipeline on multiple batches in parallel.

